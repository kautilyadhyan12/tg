// The Classes screen's two shared styles, in a plain module so the component
// files export only components (react-refresh).

export const inputStyle = {
  background: '#0A0908',
  border: '1px solid rgba(255,255,255,0.10)',
  color: '#fff',
};

export const labelStyle = { color: 'rgba(255,255,255,0.45)' };
