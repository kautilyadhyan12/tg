{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "worker": "tsx src/worker.ts",
    "dpdp:purge": "tsx tools/dpdp-purge.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test tools",
    "_lint:harness_why": "T3 round 3 C/H-1 put `node scripts/check-harnesses.mjs` on the END OF THIS LINE: a raw newline inside a mutant string had made mutate-orgs.mjs a SyntaxError, so none of its 57 mutants could run and nothing saw it (eslint ignores tools/**/*.mjs, tsc never reads it). ROUND 5, Low-6 MOVED THE CALL TO THE ROOT lint SCRIPT and it is not duplicated here: as a step in THIS task it was cached on this package's inputs, none of which reach the 16 harnesses outside apps/api, so a warm run replayed a pass without running it. The guard is unchanged and stronger; only the place it is invoked from moved. Do not re-add it here — that is what made a repo-wide check look like an apps/api one.",
    "test": "vitest run",
    "test:local": "node scripts/test-local.mjs",
    "_test:local_why": "DECISIONS :5857 rule 4a asked for a local-Postgres switch and left the saving UNVERIFIED. Measured 2026-08-21: a round-trip `select 1` is 202.9 ms median to the Neon branch in ap-southeast-1 (Singapore) and 2.7 ms to a local docker Postgres; orgs.sweep.test.ts is 158.2 s against Neon and 10.8 s locally (14.7x), and a two-suite Neon run DID NOT FINISH in 10 minutes. Use this for mutation sweeps above all — the harness runs the suite once per mutant, and the clock card's audit had its control abort twice under Singapore contention (:13336). Needs `docker compose -f infra/docker-compose.dev.yml up -d postgres redis`; the script refuses to run against a missing or unseeded database rather than failing forty tests confusingly. KNOWN AND NOT HIDDEN: the FULL api suite flakes on a fast database — nine test files call seed() against one shared database while two assert exact global counts. Pre-existing, made visible rather than caused by the switch; own OWED line. Scoped runs (a file, a -t filter) are unaffected, which is what a mutation sweep does.",
    "db:generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "seed": "tsx src/db/seed.ts",
    "coach:ingest": "tsx src/modules/coach/ingest.ts"
  },
  "devDependencies": {
    "@app/config": "workspace:*",
    "@types/jsonwebtoken": "9.0.7",
    "@types/node": "^22.10.7",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9.18.0",
    "mongodb": "^7.5.0",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@app/engine": "workspace:*",
    "@app/shared": "workspace:*",
    "@fastify/cookie": "11.0.2",
    "@fastify/cors": "^11.2.0",
    "@fastify/rate-limit": "^11.1.0",
    "@fastify/sensible": "^6.0.4",
    "@huggingface/transformers": "^4.2.0",
    "@node-rs/argon2": "^2.0.2",
    "@sentry/node": "^10.63.0",
    "bcryptjs": "3.0.2",
    "bullmq": "^5.80.10",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.10.0",
    "google-auth-library": "^9.15.1",
    "ioredis": "^5",
    "jsonwebtoken": "9.0.2",
    "pino": "^10.3.1",
    "postgres": "^3.4.9",
    "posthog-node": "^5.39.4",
    "tsx": "^4.23.0",
    "zod": "^3.24.1"
  }
}
