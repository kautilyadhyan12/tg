# ROADMAP — what we build, in order

The ordered list of work to launch on web and mobile. Kd decides the order; a chat
picks the next unticked item in the current stage unless Kd says otherwise.
Tick an item when it is merged to main and Kd has clicked through it — or, for a
card with no screen, on merge alone (RULINGS 2026-09-09). If a feature is
deferred, add it here in the same commit, never in prose elsewhere.

One lettered line is one chat and one pull request. A chat that finds its line is
still too big (it touches more than one of sign-in, money and other people's data,
or a review would have to read more than about a thousand new lines) splits the
line here first, then builds the first half. Item 1 below was four features in one
line and drew a review of that size; that is why the lines are now small.

The full previous list (304 open items, 2026-09-07) is kept at
`archive/records/OWED.md` for detail; anything from it that matters before launch
is here, the rest is under "After launch".

## Stage 0 — Reset (2026-09-07)

- [x] Archive the record files, old backends and smoke sheets; write `CLAUDE.md`, `RULINGS.md`, `ROADMAP.md`, `HANDOFF.md`.
- [x] Merge `web-repoint` into `master` (fast-forward, 2026-09-07, CI green); 47 merged remote branches and 37 local ones deleted; `web-repoint` retired. Every feature from now on is a short branch off `master` and a pull request.

## Stage 1 — The member's core, web first

1. [x] **Sign-in by email code.** One "Get started" screen: Continue with Google · Continue with email (6-digit code). Signed in at once, straight to onboarding. Sign-in doors relabelled "Train" and "Manage my gym, studio or clients". No password at all (Kd, 2026-09-07 — the optional-password and reset-screen halves are struck); verify-email step gone (a code proves the email); deleting an account confirmed by an emailed code. Email through Resend (the service is the dependency; no new package). *Built 2026-09-07; three review rounds; merged to `master` 2026-09-08 (PR #51, CI green).*
2. **Organisation types** (split 2026-09-08).
   - 2a. [x] **Types at creation.** Gym · studio · personal trainer; a new owner from the "Manage" door lands on "create your organisation" with no personal onboarding. *Merged 2026-09-08 (PR #53).*
   - 2b. [x] **Words by type, everywhere else.** Every console screen, the member's
     join door and My Gyms speak the organisation's own words — Clients / Coach for a
     studio and a personal trainer where a gym says Members / Trainer (spec Part 3 §2.2).
     One table in `@app/shared`, read by the API and by every screen, so the server's
     refusals and the screen agree. *Merged 2026-09-08 (PR #55).*

3. **Onboarding v2, server side** (split 2026-09-08; the science first because every screen shows its number).
   - 3a. [x] **The plan maths and its sanity rules.** Resting burn, daily activity + training days, pace → deficit or surplus, macros, finish date; target direction, healthy-weight floor, over-a-year pace, the calorie floor; the under-18 and flagged-condition rules (no deficit). Pure functions, a test for every rule. Nothing on screen yet; the click-through says so.
   - 3b. [x] **Health screening, Safe mode and the consent log, server side.** ONE general question stored as a yes/no plus the "Check first" choice, changeable any time (RULINGS 2026-09-09 — no named condition, ever); Safe mode and "no calorie cut" derived on the server and read by the plan maths (`hasCondition` + `safeMode`, reasons `health_answer` / `safe_mode`) and by the live macro rings; the consent log (time, app version, wording verbatim) for sign-up, the health step and the plan screen. Tenancy test on every route. *Built 2026-09-09, branch `health-screening`; review round 1 fixed the same day; Kd ruled on the consent log (kept six years, sweep built) and the old conditions box (off at 4b); re-check done the same day (one High — the consent limiter throttled a whole gym — five Low, all fixed); round-2 re-check found no Critical/High (four Low + three test gaps, all fixed). No click-through: 3b has no screen (RULINGS 2026-09-09). Merged 2026-09-09 (PR #57, CI green, branch deleted).*
   - 3c. ~~Allergen tags on every food.~~ **Struck 2026-09-09** (RULINGS): no tags, no allergy question; every meal suggestion carries a food-allergy caution line instead, built with 7a. (Contraindication tags on exercises were struck the same way: with one general health question the app cannot know which exercises to exclude, RULINGS 2026-09-09; a cleared person gets the normal plan with a "follow your professional" line, built with 6a.)
4. **Onboarding v2, screens** (split 2026-09-08 along the twelve screens in RULINGS; master is not live, so a half-built flow may land and the next half continues it).
   - 4a-i. [x] **The answers and the live plan, server side** (split 2026-09-09: seven screens plus a migration, two routes and the macro rings' move is more than one chat and more than a reviewable diff). Stores what the screens ask — the one main goal, pace, the day, the two fitness checks, the week, equipment — saved as you go, so a screen never wipes an answer it did not ask about. `GET`/`PATCH /v1/users/me/onboarding` answer with the plan numbers or the honest list of what is still missing; "today" comes from the device's time zone and the server clock, never a body field. Nothing on screen yet, so no click-through (RULINGS 2026-09-09). *Built 2026-09-09, branch `onboarding-plan-server`; six review rounds, the last with no Critical/High and its re-check waived by Kd. Merged 2026-09-10 (PR #58, CI green, branch deleted).*
   - 4a-i-b. [x] **Weight has one source and no copy** (Kd's redesign, 2026-09-10: the profile form echoing a weigh-in's number left a "typed by me" entry the person never typed, so deleting the mistaken weigh-in kept its number). Migration `0027` drops the `users.weight_kg` cache after re-running 0026's safety backfill; every reader computes the weight from the history. No screen changes, so no click-through (RULINGS 2026-09-09). *Built 2026-09-10; Kd waived the re-check (no Critical/High). Merged 2026-09-10 (PR #59, CI green, branch deleted).*
   - 4a-ii. [x] **Screens 1–7:** goal · about you · target · your day · your training (push-up and plank checks) · your week · equipment. Tap-only, saved as you go on the 4a-i routes, the live plan number on every screen from the moment it exists (after "your week", screen 6, when the eight core answers are in; before that no number box at all, RULINGS 2026-09-10 — no number from a default, RULINGS 2026-07-15). They replace the old five-step form; its free-text conditions box stays in Settings until 4b switches it off (RULINGS 2026-09-09). Screen 7 makes "no equipment" exclusive as you tap, AND drops "none" as it LOADS an answer the old form already wrote: the v2 contract refuses "none" beside real equipment, the old free multi-select can still send that pair, so without this a person carried over from it taps Save without touching equipment and gets a 400 with nothing on screen to explain it (the old form is left alone until this screen replaces it). Also here: `onboardingCompleted` is stored exactly as the client sends it, so `true` with no answers saved opens the training side (the v1 PUT has always been the same, and nothing on the server reads that column as a gate) — the server refuses `true` while `missing` is non-empty, which is what RULINGS 2026-07-19 asks for. The v1 PUT, which only Settings writes once these screens replace the old form, still stores the flag as sent. *Split 2026-09-10: the macro rings' move is 4a-iii. Built 2026-09-10 on branch `onboarding-screens-1-7` (PR #60); Kd's click-through passed and his look changes landed the same day (RULINGS 2026-09-10). The five fixes from his second look were built the same day (RULINGS 2026-09-10): flick wheels, the name first on "About you", plain push-up and plank questions with "Not sure", "How is this worked out?" from the server's own steps, two drawn icons. With them, the plan's daily burn became the exact sum of the whole-kcal steps it prints, and the finish date is counted in whole hundredths of a kilo (a floating-point crumb had put some dates a day late). Kd's second click-through found a target on the wrong side of the weight accepted (fixed the same day, RULINGS 2026-09-11). Review round 1 (2026-09-11): 3 High, 6 Low, all fixed — the wheels no longer save a number the person did not pick, and the plan's working must multiply out as well as add up. The re-check the same day found no Critical/High and four Lows, all fixed: a slow drag saves where the finger lifts, and every figure the plan panel prints twice must agree. Kd's click-through of the wheels passed (RULINGS 2026-09-11: tap a wheel before flicking it, kept). Merged 2026-09-11 (PR #60, CI green, branch deleted).*
   - 4a-iii. [x] **One daily-calories number: the macro rings read the plan.** The nutrition targets route (`/v1/nutrition/targets`, shown by the web's macro rings) moves onto the stored plan answers through the one function the onboarding routes use — before, it read the old profile with a fixed −400/+300 and could differ from the number screens 6–7 show by a few hundred kcal. Someone who finished the old form has no one goal and no "your day", so the rings' honest empty state names the questions still open and "Answer now" opens onboarding on the first of them; a person who already finished also gets "Back to the app" there, beside "Sign out", and a name changed on the way (or in Settings) reaches the sidebar and the dashboard's greeting at once. Protein (Kd asked for the science, RULINGS 2026-09-11): grams per kilo, never a share of the calories, counted at the BMI-30 weight for a heavier body, each figure's source named in "How is this worked out?". Settings' goal chips still show the `fitness_goals` list, so until 4a-iv both save routes keep it and the one goal in step, and a change takes effect at once (RULINGS 2026-09-11): ticking Weight Loss or Muscle Gain unticks the other; on a Settings save the ticked one sets the calories, else the main goal stays while it is ticked, else the first goal ticked; screen 1's goal goes first in the list and only a goal that fights it leaves. A target left on the wrong side of the weight (the goal changed, or the target reached) makes the rings ask for a new one, never call it unanswered nor show the held weight as the goal's number, and Settings' target box refuses one typed in, judged by the goal the calories follow (RULINGS 2026-09-11). Comes BEFORE 4a-iv: the rings must read the plan before the goals move underneath them. *Built 2026-09-11 on branch `rings-read-the-plan` (PR #61). Kd's click-through passed; merged 2026-09-11 (PR #61, CI green, branch deleted).*
   - 4a-iv. [x] **Many goals, and a gym.** Screen 1 becomes ONE weight choice (lose · keep · gain — it alone sets the number) plus "also work on", any number (RULINGS 2026-09-10). A `weight_goal` column with its CHECK replaces `main_goal` by migration, backfilled from today's goal (weight loss → lose, the goals that keep the weight → keep; the people who chose build muscle are asked, never given one: Kd's answer, RULINGS 2026-09-11); the old goal joins the "also" list unless it was weight loss. `fitness_goals` becomes the "also" list, its enum losing weight loss and gaining get stronger and better balance; the v1 profile PUT and Settings' goal chips take the same two questions, which ends the goal list 4a-iii keeps in step with the one goal from both sides; Settings' "Reset onboarding" then wipes the v2 answers too (pace, your day, push-ups, plank: RULINGS 2026-07-20 — today its `PUT {}` keeps them while its confirm box promises a blank wizard); `PLAN_GOAL_BY_MAIN_GOAL` goes, the direction is stored, never derived. "Build muscle" ticked raises protein to at least 2.0 g per kg; nothing else in the calorie maths reads the "also" list. Screen 7 gains "A gym (everything there)" beside the home equipment, a new equipment value with its CHECK (Kd said yes, RULINGS 2026-09-10, decision B). *Built 2026-09-11 on branch `many-goals-and-a-gym`; at Kd's click-through screen 1 became one grid three to a row under "Your goal", a ninth goal Stay healthy joined, and "A gym" lost "(everything there)" (RULINGS 2026-09-11). Migration `0028` (the weight choice with its CHECK; the goals and the equipment lists CHECKed, "no equipment" alone; `main_goal` dropped); Build muscle ticked sets protein to 2.2 g per kg, Morton 2018's figure (the line above said 2.0); Settings asks screen 1's two questions and screen 7's equipment with the screens' own line icons; "Reset onboarding" is `DELETE /v1/users/me/onboarding`, which clears every answer on the profile, the ones only the screens ask included, and keeps the name, the weigh-ins and, until 4b, the health answer. Kd's click-through passed the same day. Merged 2026-09-12 (PR #62, CI green, branch deleted).*
   - 4b. **Screens 8–11** (split 2026-09-12: four screens, a Settings screen, the running columns and two migrations is more than one chat and more than a reviewable diff).
     - 4b-i. [x] **Screen 8, health — the ONE question**, on the 3b routes: the yes/no, "Check first" (cleared · not yet) and the disclaimer tap. The question's words gain "or take any medicine, including for weight loss" (Kd's decision C, RULINGS 2026-09-10). Settings gets the same question, in the SAME component, so the answer can be changed later and everything follows at once (RULINGS 2026-09-09). Finishing setup is refused while it is unanswered (`missing: ["health"]`), which is why Settings' "Reset onboarding" now clears the health answer too — the question is back in the wizard, so a cleared yes has a screen to be given again on. The old free-text "medical conditions" box (Settings) goes with its untrue hint "Helps the AI give safer advice" (RULINGS 2026-08-18), and `user_fitness_profiles.medical_conditions` is DROPPED with everything typed in it by migration `0029` (RULINGS 2026-09-09). The three disclaimer wordings in `packages/shared/src/health.ts` gain a v2 that keeps "follow their advice" and loses the comparison with the app, so the consent log keeps what each person agreed to (RULINGS 2026-09-10, decision D). *Built 2026-09-12 on branch `health-question`; Kd's click-through passed the same day (*"smoke passed"*). Three review rounds, each finding less: round 1's High was the note under the question promising the app keeps less than it keeps; the first re-check's High was a finish the server refuses leaving a stale answer on screen, which the tap could not replace; the last round found no Critical/High and three Lows, fixed here — no health answer is taken while a finish is out (a refusal would forget one the server had just stored), the note promises no detail AT ALL rather than naming the condition alone (the question also asks about medicine, an injury and pregnancy), and the unanswered screening is one shared constant that crosses the contract, read by the server and both screens instead of hand-written in each. Two tests that could not fail were fixed with it: the build's own `VITE_APP_VERSION` now goes through the clip (the rule was tested, the wiring was not), and "the health question is above the fitness form" now pins the whole form, not just its Save button. Merged 2026-09-12 on Kd's word — *"fix and merge"* (PR #63).*
     - 4b-ii. [x] **Screen 9, food, and screen 11, the gym code.** (A code that arrives on a poster link is applied first: 4b-ii-b.) Food asks the diet — vegetarian · vegetarian with eggs · non-vegetarian · vegan (RULINGS 2026-09-10) — and meals a day. NO cuisine question and no cuisine data (RULINGS 2026-09-12): the food table holds 131 foods with no cuisine label on any of them, and suggestions (7a) read the whole table by diet and the calories left. *Built 2026-09-12 on branch `food-and-gym-code`. Migration `0030` adds `diet` and `meals_per_day` with their CHECKs, both NULL for everyone (nobody was ever asked, and a guessed diet would put meat in front of a vegetarian). Both answers gate the finish, in the shared enum beside `health`; the gym code never can, because most people have no code. Screen 11 draws the join door's own two components, the ones Settings' Gym tab draws, so three places cannot answer differently — its "Back to your dashboard" is hidden mid-setup, where `ProtectedRoute` makes it a loop. Finish moved to the last screen with them, so the disclaimer tap on screen 8 is now named and reachable from there ("Read the note on the Health screen and tick it" + Go to Health), and Back is held while a finish is out, as the step bar already was. Settings' Fitness tab asks both questions, in the screen's own words and icons. Changed 2026-09-13: the profile PUT refuses to finish setup while an answer is open, as the onboarding PATCH does, and keeps a finish already stored, so Settings still saves; the meal ideas after a workout follow the diet (Kd, RULINGS 2026-08-06 as amended) and carry the food-allergy caution; screen 11's "Try again" goes to the code box below instead of back into setup; Settings offers "Not set" for meals only while none is stored. Kd's click-through of the fixes passed 2026-09-13 ("all passed"). Changed on this branch from that click-through (ce4420e, 2026-09-13; RULINGS 2026-08-06 as amended 2026-09-13): the meal ideas' words in `apps/api/src/modules/workouts/summaryContent.ts` and their tests — over 400 kcal: "Protein shake + banana" (Within 30 mins; vegan "Plant protein shake + banana") · "Grilled chicken + potatoes + vegetables" (Within 2 hours; vegetarian and below "Grilled tofu + potatoes + vegetables") · "Greek yogurt with berries" (**In 3–4 hours** — never "before bed", nor "Later today", which does not count from the workout (Kd, RULINGS 2026-09-13); vegan "Soy yogurt with berries"); over 200: "Protein shake or chocolate milk" (Within 30 mins; vegan "Plant protein shake or soy chocolate milk") · "Eggs + whole grain toast + avocado" (Within 2 hours; vegetarian and vegan "Tofu scramble + whole grain toast + avocado"); the rest: "Banana + peanut butter" (Within 30 mins, every diet) · "Light salad with grilled protein" (Within 2 hours; vegetarian and below "Light salad with chickpeas"). Same bands, same thresholds; the unit test's "verbatim port" claim becomes "the market's words, RULINGS 2026-09-13". Kd's click-through of the last fixes passed 2026-09-13 ("all passed"); merged the same day (PR #64).*
     - 4b-ii-b. [x] **A poster's code survives sign-in and setup, and is applied first** (split from 4b-ii, 2026-09-13, because it goes through the sign-in door). On 2026-09-07 Kd agreed that "the gym code is applied first" means *"so they are joined even if they stop halfway"* (RULINGS 2026-07-19, amended that day). Today `/org/join?code=…` loses the code twice: signed out, the login page lands the person where it decides and never returns to the address; signed in but not finished, `ProtectedRoute` sends them to setup without it. Carry the code through both, and put it in front of the person before screen 1 — filled in, never sent without their tap (a poster must not enrol whoever scans it). The plan asks Kd what that first step looks like. *Built 2026-09-13 on branch `poster-code`. Kd picked "Your code" moved to the front of setup (RULINGS 2026-09-13). The code is kept in the tab as the door is (`CarryJoinCode` on the join address; Google leaves the site and comes back), and only a code the server could have made is kept (`linkedJoinCodeSchema` in `@app/shared`), so an edited link cannot put its own words on the sign-in page. Signing in lands a set-up person on the join page with it and anyone else in setup, where Continue from the code goes on to the first open question. The kept code is forgotten when sent, on reaching the join page, when a newer link carries no code, and on sign-out; a code never sent goes with Finish to the join page, still unsent (Kd, RULINGS 2026-09-13, from the review). No change in what the server does. Kd's click-through passed 2026-09-13 (*"all passed"*), and again for the Finish change the same day (*"all passed"*). Merged 2026-09-13 (PR #65, CI green, branch deleted).*
     - 4b-iii. ~~Running, asked and never tested.~~ **Moved out of setup to 7b, 2026-09-13** (Kd, RULINGS 2026-09-13: setup asks nothing about running, as Strava's sign-up does). Its questions go with it, asked when someone opens the running feature: "How long can you jog without stopping?" — I don't jog · under 5 minutes · 5 to 15 · 15 to 30 · over 30 — tap-only, "Not sure" allowed, stored as a band; "Do you run, or want to start?"; and, for "I run", the runner's own: days a week now · usual weekly distance · longest run in the last month · a recent time over a known distance, optional (5K · 10K · half · marathon).
   - 4c. [x] **Screen 12, your plan:** live numbers, the disclaimer, Adjust, and the Safe mode version that shows no workout or run plan. *Built 2026-09-13 on branch `plan-screen` (PR #66): the last screen, after "Your code", with the week of workouts (Safe mode's words in its place), every answer with an Adjust, and its own disclaimer tap, which Finish waits for. Kd's muscle ruling (RULINGS 2026-09-13) is built with it; no migration, no new route. The review and its re-check found no Critical/High; the re-check's fixes were not re-checked, on Kd's word. Kd's click-through passed 2026-09-14 (*"all test passed"*). Merged 2026-09-14 (PR #66, CI green, branch deleted).*
   - 4c-ii. [x] **A target under the healthy weight is said on "Your target", as it is picked** (Kd, 2026-09-14, from 4c's click-through; RULINGS the same day). *Built 2026-09-14 (PR #69).* The moment the target wheel lands under the lowest healthy weight for the height, a panel under it says so, and what the plan does: it will not take the person below that weight, or, for someone already there, will not lower their weight. The target can still be picked, the number box keeps its line, and Settings' target box says the same. At 16 and 17 the figure is the teen one (Kd's answer) and the words say "for your height and age". Each screen says it only where the target reads under that weight as that screen shows it, so a target in pounds that reads as the healthy weight is never called under it. Kd's click-through passed 2026-09-14 (*"all test passed"*). The review found no Critical/High; its four Lows and three weak tests were fixed the same day, and the fixes were not re-checked, on Kd's word (*"merge"*). Merged 2026-09-14 (PR #69, CI green, branch deleted).
   - 4d. [ ] **The sign-up disclaimer tap** (found 2026-09-13 while building 4c). RULINGS 2026-09-07 asks for one explicit tap at sign-up, stored with the time, the build and the words, as the health step's (4b-i) and the plan screen's (4c) are. The server has held the `sign_up` wording (v2) and the consent route since 3b, but no screen shows the words and nothing records the tap. The plan asks Kd where it sits (a Google sign-in leaves the site, and the tap can only be stored once the account exists).
5. **Goals and the Today screen** (split 2026-09-08).
   - 5a. [ ] **Goals, server side.** The goal object (type, target, period day/week/month/year, app-set or user-set), the starter set from onboarding answers, progress from existing data. Routes with the stranger-gets-404 test.
   - 5b. [ ] **The Today screen.** Rings from progress, the goal list, change, remove and add. The rings say why a plan holds no calorie cut (under 18, a yes on the health question, Safe mode: the plan's `no_deficit` reasons); the setup screens say it since 4a-ii, and the Nutrition page's rings still do not.
6. **Recommended workout and the weekly plan** (split 2026-09-08).
   - 6a. [ ] **The plan builder, server side.** Shape from days and level; exercises by goal, equipment and health limits; numbers by level and goal; push-up and plank checks set the starting numbers (skipped = the self-rating alone sets them); progression to the harder variant once mastered; Safe mode builds no plan. Pure and tested. Several goals (RULINGS 2026-09-10): each "also work on" goal adds its own kind of work — build muscle and get stronger: lower reps and progressions · get fitter and endurance: circuits and intervals · flexibility: mobility blocks · posture: back and core · better balance: single-leg work · stress relief: slower sessions with a breathing cool-down · stay healthy (Kd added it at 4a-iv's click-through): the WHO's weekly activity guidance (WHO, 2020), checked against its own text when 6a is built; the week's sessions are shared out across the ticked goals and the plan screen says how much each gets; with more goals than sessions, goals share a session. The calorie number never reads the "also" list.
   - 6b. [ ] **The weekly plan screen.** The week, swap, move, add. Adds the **Exercise** option to the dashboard's Recommended box (7a-ii): today's session from the week's plan, and for a person in Safe mode why there is none. (The dead old-backend "Recommended for you" box itself is replaced at 7a-ii.)
   - 6c. [ ] **Doing a planned workout.** Camera counting where the engine supports the exercise, guided otherwise; a finished workout feeds progression.
7. **Meal suggestions and the run plan** (split 2026-09-08; 7a-i and 7c added 2026-09-12 on Kd's asks; 7a-ii added 2026-09-13). Order (RULINGS 2026-09-12): after the setup screens come 7a-i, then 7a and 7a-ii, then 7c — 7c swaps meals out of 7a's suggestion, so 7a must exist first; 5, 6 and 7b follow in their numbers.
   - 7a-i. [ ] **A bigger food list, and every food's diet** (Kd, 2026-09-12). The curated table is code, not a table: `apps/api/src/modules/nutrition/foods.ts`, 131 foods (counted 2026-09-12), each with its calories and macros per 100 g. It grows to about 300 — vegetables, fruits, pulses, whole grains, dairy, fish, eggs, nuts and the everyday dishes people actually log — LEADING with meat, fish and eggs, because the market is the US, Canada and Europe (RULINGS 2026-09-12), with every number's source named. Every food gains its diet level (vegan · vegetarian · with eggs · non-vegetarian), which is what makes screen 9's answer mean anything: without it a diet can be asked and nothing can be filtered by it. It also narrows the meal scanner's honest gap — an item our list does not know is dropped from the totals and named ("Couldn't identify: …"), and a bigger list is the only thing that closes it. Comes BEFORE 7a.
   - 7a. [ ] **Meal suggestions.** From the food table by diet, meals a day and calories left (no cuisine filter and no cuisine data, RULINGS 2026-09-12); a food-allergy caution line on every suggestion (RULINGS 2026-09-09 — no allergen tags, no allergy question; its words are `FOOD_ALLERGY_CAUTION` in `@app/shared`, already under the meal ideas after a workout); under-18 and Safe mode rules respected. Someone with no diet or meals answer (everyone who finished setup before 4b-ii) is asked for it, never given one. The post-workout card's ideas (`summaryContent.ts`) come from this same engine once it exists — what to eat now, given what the workout burned and what is left today — so there is ONE source of food ideas; decided in this card's plan. The ideas VARY: drawn from the food list afresh each day, and per workout on the post-workout card, never the same lines every time — Kd asked on 2026-09-13 whether the post-workout ideas repeat each day, and today they do (seven fixed lines in three bands) until this card replaces them. A non-vegetarian's suggestions include vegetarian and vegan dishes; the diet is a ceiling, never a demand (RULINGS 2026-09-13).
   - 7a-ii. [ ] **The dashboard's Recommended box, with options** (Kd, from his click-through of 4b-ii's fixes; RULINGS 2026-09-13). Today the dashboard's "Recommended For You" box reads the OLD backend's `/recommendations` (`recommendationApi.js` → `mlApi`), which is down, so it prints "Recommendations are unavailable right now." — item 8's class of dead screen. It becomes ONE box with a row of options across its top — **Exercise · Food · Running** — and the tapped option fills the box with that kind of recommendation; nothing else on the dashboard moves. Built here with its first option, **Food**, reading 7a's suggestions (the same engine, never a second one). **Exercise** joins at 6b (today's session from the week's plan) and **Running** at 7b. An option whose plan is not built yet is not shown, so the box never promises a kind the app cannot make; and the box says, in plain words, what each kind is and why it is empty (Safe mode, no run answer). The post-workout card stays as it is. **Two lines to Kd in this card's plan, before building:** (1) the design as read above — is that it? (2) Running on the WEB: RULINGS 2026-08-24 sent running to the phone app ("available in the app") and 7b's line says the same, but his ask names Running as an option on the web dashboard; recommended: the web shows the week's runs too — reading a plan needs no GPS, only recording a run is phone-first.
   - 7b. [ ] **The run plan.** A week's run schedule from the running answers, which are asked here when someone opens running, not in setup (RULINGS 2026-09-13; the questions are on the 4b-iii line) (phone app; web says "in the app" — unless Kd rules at 7a-ii that the web shows the week's runs); adds the **Running** option to the dashboard's Recommended box (7a-ii); none in Safe mode. The rules the running community accepts (RULINGS 2026-09-10): a person who cannot yet jog 30 minutes gets a walk-run progression (the Couch-to-5K pattern the NHS publishes: three days a week, run intervals grow and walk breaks shrink over about nine weeks, starting from the jog band they gave); a runner starts at the weekly distance they run NOW, never more, and it grows gradually (about 10 % a week at most) with an easier week every third or fourth; one long run a week; most running easy and at most one hard session a week for a beginner, two for an experienced runner; a rest or easy day after a hard one; training paces only from a REAL recent time (Jack Daniels' published VDOT formula), never guessed — no recent time means paces by feel ("easy: you can talk"); no run plan for a "no" to "Do you run, or want to start?"; none in Safe mode; the plan builder's circuits (6a) start from the fitness, push-up and plank answers, since setup no longer asks the jog band. **To settle in this card's plan** (Kd, 2026-09-13, said in passing, not yet a ruling): *"i think running recommendation is not needed but we will have a option in the running feature i mena the page where there will be some advice to go from a beginner to a pro but it will be build in the mobile apps not the browser"* — a plan made from the answers, a beginner-to-pro advice page, or the page shaped by the answers; and how that fits the Recommended box's Running option (RULINGS 2026-09-13, 7a-ii).
   - 7c. [ ] **Your own diet plan** (Kd, 2026-09-12): the app's suggestion is a starting point, never the only choice. Swap a meal, add your own, change a portion, and keep it as your plan for the week — the way the weekly workout plan (6b) and the run plan (7b) are edited. Nothing like it exists: no meal-plan table, no routes, no screen, so this splits into the plan and its rules on the server and then the screen when it is built. The day's calorie and macro targets are the plan's rails, and Safe mode and the under-18 rule hold here as they do everywhere (RULINGS 2026-09-07).
8a. [ ] **What a gym can see, in the person's own terms** (Kd's click-through of 4b-ii, RULINGS 2026-09-13). `components/gym/OrgVisibilitySheet.jsx`, drawn on the join door, onboarding's screen 11 and Settings → Gym. The AI-coach line and the "before you joined, or after you leave" line go. Meals and nutrition, weight and body measurements, and run routes stop being flat "Never" and become "only if you choose to share". **Raise with Kd in this card's plan, with a fix and a recommendation:** there is no sharing control anywhere in the app today, so "if you choose to share" describes a choice a person cannot make — the same class of false-on-screen line the old "Helps the AI give safer advice" hint was. Recommended: word it so it is true today and stays true when the control lands ("never — unless you choose to share it"), and build the control itself as its own line rather than inside this one. Kd's exact words are in RULINGS. Also on this card: Kd quoted the join panel's "If you're standing at the front desk, ask them now — it takes one tap" and did not say what should change about it — ONE line to him before building, never a guess.
8. [ ] **Dead web screens off the old backend**, one screen per chat. Predictions, workout templates, profile photo (needs image storage), badges/challenges/leaderboard "coming soon" — each either moved to the new API or shown as "in the app", never left broken. (The dashboard's dead "Recommended For You" box is 7a-ii, not here.) Delete account moved to the new API on 2026-09-07, with the undo page (`/restore-account`) built the same day after review.
9. [ ] **Chat coach switched off** on the web (route and import), cost counter moved out of the coach service. Kd's ruling; still live and billing today. **Ask Kd in this card's plan:** on 2026-09-13 he said *"the chat part will be completely deleted"*, while RULINGS 2026-08-18 keeps the coach off, not deleted from the code.
10. [ ] **Known defects on the way**, one defect per chat, each starting with a failing test: a network blip on page load logs you out · a dropped request logs you out instead of offering retry · sync upload has no timeout · the full API suite flakes because nine test files share one seeded database · a workout's calories use the weight on the day it syncs, not the day it was done (spec Part 4 §3.6 asks for the newest weigh-in at or before the workout; the two differ only for a workout synced after a later weigh-in) · the web test `xpDisplay.render.test.jsx` "a non-array recommendations field does not blank the page" reads the recommendations message the moment "Level 3" shows, without waiting for it, so a slow CI runner fails it (seen once on PR #60) · the web test `settingsHealth.render.test.jsx` "says so when the health answer cannot be read, and Try again reads it again" waits the default one second for "Try again" and fails in the full local run while passing alone (seen 2026-09-13, on the committed 4b-ii code too).

## Stage 2 — The gym console, remaining

Order (Kd, 2026-09-13; RULINGS the same day): **items 3 and 5 first** — the email invite is how a US gym gets members onto an app; the numbers below are kept so nothing that names them goes stale.

1. [ ] Gym leaderboard (R1–R18 of 2026-09-07): one ranked list, filters, member's My Gyms view, goal points, greyed opt-out, info symbol.
2. [ ] "Slipping away" web half + message expiry after 7 days (server half is built, unreviewed).
3. [ ] Roster import: CSV + XLSX upload, column-mapping preview, attach-on-join — and the EMAIL INVITE (Kd, 2026-09-13): the owner types one address or imports many, the app emails each "you're a member of {gym} — get the app" with the join link, and a person who signs in with that email is confirmed on their own (RULINGS 2026-08-18). First in this stage.
4. [ ] Staff invites by email; a gym can be handed to another owner; a gym can change its own name, city, timezone.
5. [ ] Members screen contents (last active, streak, search, CSV export); notify members when confirmed or removed.
6. [ ] Gym profile page for members; gym announcements feed; gym greeting and branding on the member home.
7. [ ] Member-to-coach / gym messaging.
8. [ ] Classes, schedules and booking (the largest gym item).
9. [ ] Gyms set their own prices, offers and promos; gyms sell products to members.
10. [ ] Coach-authored workout and diet plans for a named member; coach videos (20 per gym, 1 minute).
11. [ ] Photo sharing to the gym (one-week life, reactions only, report-and-remove), the photo/stats editor.
12. [ ] Reports section; billing section buttons; the seat cap re-counted; the two sweeps run in production configuration.
13. ~~The invite card~~ — **moved to Stage 5 item 7** the same day it was planned (Kd, 2026-09-13: *"lets build these things when we develop the mobile app not in web app"*). What the US market does instead, for the main path, is items 3 and 5 here: the owner types or imports members' emails, the app emails "you're a member of {gym} — get the app", and the person who signs in with that email is confirmed on their own (RULINGS 2026-08-18: verified-email match auto-confirms). Kd agreed 2026-09-13 (*"i agree with you"*): those two come FIRST in Stage 2, before the leaderboard — the order note at the top of this stage.
14. [ ] Kd's admin panel — last.

## Stage 3 — Money

1. [ ] Paddle checkout for individuals and gyms; the subscription state machine; webhooks (raw body, dedupe, worker); invoices.
2. **Trials enforced** (split 2026-09-08 after Kd's trial-abuse ruling; no approval queue, ever).
   - 2a. [ ] Trial lengths 7 days individual and 15 days organisation (the button still says 30), one per owner account (built), no card; the price book seeded as ruled; bands 3–5 USD and the INR book to Kd if he wants to revisit.
   - 2b. [ ] Same person, new spelling: Gmail dots and `+tags` collapse for trial counting; throwaway email domains refused at the Manage door.
   - 2c. [ ] One device, one trial, whatever the email: a browser mark on the web, the stores on the phone.
   - 2d. [ ] The trial belongs to the gym: a new organisation whose first members were members of an already-trialled organisation gets no second trial and sees the subscribe prompt; the owner's own device counts as member one.
   - 2e. [ ] Allow now, look later: the "have a look" list for weaker clues (an email to Kd until the admin panel; pause and remove already exist) and the second-trial code for a real owner of two gyms.
3. [ ] Gym-collects-from-members (Stripe Connect) — after launch unless a gym asks.

## Stage 4 — Launch readiness

1. [ ] Deploy: a server, a domain, secrets in the platform, automatic deploy from main, health checks, the worker running (DPDP day-14 purge must run before the first real sign-up). The web build sets `VITE_APP_VERSION`, which every consent row stores as the build that showed the words (4b-i); until it does, a build says `web-dev`.
2. [ ] Backup and restore drill performed; the secrets escrow list.
3. [ ] Consent screens for health data and the camera; terms and privacy notice reviewed by a lawyer; company and insurance (Kd).
4. [ ] Image storage on R2 (meal photos with 7-day deletion, avatars, gym logos).
5. [ ] The pricing/allowance questions still open with Kd: bands 3–5 and INR after the raise; does the gym raise move the individual tier; the yearly book; "custom above 2,100" has no route in.
6. [ ] Camera accuracy list (from the archive): rest billed as squatting, a too-shallow squat says nothing, constant voice coaching, 12 vs 15 frames a second, person-check tuned on a different model, degradation ladder, choosing which person is the user, follow-the-demo mode.

## Stage 5 — The phone app

1. [ ] Expo dev client; the two-day pose spike (≥20 fps on a mid-range Android is the gate).
2. [ ] Screens on shared packages: sign-in (code, Google, Apple), onboarding v2, Today, goals, plans, library, camera workout, progress, My Gyms, console.
3. [ ] Offline SQLite mirror and the sync engine.
4. [ ] Running end to end: MapLibre + OpenStreetMap tiles, background tracking, the Play permission flow, routes, published routes only.
5. [ ] Meal scan and barcode on the phone.
6. [ ] Store billing (RevenueCat), push, EAS release trains, store listings and privacy labels, Apple small-business programme.
7. [ ] **The invite card** (Kd, 2026-09-13; RULINGS the same day, amended twice: built with the phone app, not on the web now; the app's QR stays). An organisation downloads it from the console (web and phone — the console is on both) and puts it up in the gym; never shared on social media; Copy stays for sending the code personally. **On it (Kd's words):** the organisation's logo big, or its name in big letters when it has none (Stage 4 item 4 brings the upload; `gyms.logo_key` exists and nothing writes it) · a large QR to JOIN, holding `/org/join?code=…` — the address 4b-ii-b made work for everyone — with "Scan to join {name}" and, under it, "or type the code {CODE} in the app" (the chat's choice: the join box's own hint says "read it off their poster", and not every phone scans) · a QR EACH for the Android and iPhone downloads (RULINGS 2026-09-13), each AS BIG as the join QR (Kd: a small one is no use, and a member with no app can use nothing), with the app's mark and its name "AI Home Gym" — built here, at Stage 5, the store listings (item 6) exist, so each opens a real store page. White with the app's orange so it prints cheaply; the organisation's logo or name the biggest thing on it, the app's smaller; premium, no emoji; rendered headless and looked at before Kd sees it. Made in the browser from what the console already reads; A4 at print quality; the button offered only for a live code, and Rotate says a printed card goes dead with the old code. Two free packages, both MIT, needing Kd's yes then: `qrcode-generator` (draws the QR; no dependencies of its own) and `jsqr` (tests only: reads each QR back the way a phone does, so a test proves the join QR opens the join address and each store QR its store). Tests: the card's words and what it carries from the organisation's row; all three QRs decode to their exact addresses; the button withheld for a paused, expired or used-up code; the download makes a PNG.

## After launch

- The Pact (random matching, squads, identity reveal, joint photo) · territory capture (individual perk, 200 m end-trim on routes) · published routes and the running social layer · wearables both ways · the global leaderboard · City League · TV mode · share cards · nearby gyms · a map of gyms · exercises 4–58 through the production line (12 solid before pitching) · Hindi and Assamese exercise text · the eight residual coach items (only if the coach ever returns).
- Undecided, do not build: day passes · paid friend/family invites · training together live · nearby-runner connection.

## Ruled out — never build, never re-propose

The AI chat coach (off, not deleted) · per-seat pricing · phone OTP at sign-up · gym-team territory · the eight struck gym ideas (see RULINGS) · session names · join-code labels · user-added exercises · Google Maps · desktop webcam capture for meal photos · clinics as an organisation type · Kd approval before a gym can start · a pooled route heatmap.
