# ROADMAP — what we build, in order

The ordered list of work to launch on web and mobile. Kd decides the order; a chat
picks the next unticked item in the current stage unless Kd says otherwise.
Tick an item when it is merged to main and Kd has clicked through it — or, for a
card with no screen, on merge alone (RULINGS 2026-09-09). If a feature is
deferred, add it here in the same commit, never in prose elsewhere.

One lettered line is one chat and one pull request. A chat that finds its line is
still too big (it touches more than one of sign-in, money and other people's data,
or a review would have to read more than about a thousand new lines) splits the
line here first, then builds the first half. Item 1 below was four features in one
line and drew a review of that size; that is why the lines are now small.

The full previous list (304 open items, 2026-09-07) is kept at
`archive/records/OWED.md` for detail; anything from it that matters before launch
is here, the rest is under "After launch".

## Stage 0 — Reset (2026-09-07)

- [x] Archive the record files, old backends and smoke sheets; write `CLAUDE.md`, `RULINGS.md`, `ROADMAP.md`, `HANDOFF.md`.
- [x] Merge `web-repoint` into `master` (fast-forward, 2026-09-07, CI green); 47 merged remote branches and 37 local ones deleted; `web-repoint` retired. Every feature from now on is a short branch off `master` and a pull request.

## Stage 1 — The member's core, web first

1. [x] **Sign-in by email code.** One "Get started" screen: Continue with Google · Continue with email (6-digit code). Signed in at once, straight to onboarding. Sign-in doors relabelled "Train" and "Manage my gym, studio or clients". No password at all (Kd, 2026-09-07 — the optional-password and reset-screen halves are struck); verify-email step gone (a code proves the email); deleting an account confirmed by an emailed code. Email through Resend (the service is the dependency; no new package). *Built 2026-09-07; three review rounds; merged to `master` 2026-09-08 (PR #51, CI green).*
2. **Organisation types** (split 2026-09-08).
   - 2a. [x] **Types at creation.** Gym · studio · personal trainer; a new owner from the "Manage" door lands on "create your organisation" with no personal onboarding. *Merged 2026-09-08 (PR #53).*
   - 2b. [x] **Words by type, everywhere else.** Every console screen, the member's
     join door and My Gyms speak the organisation's own words — Clients / Coach for a
     studio and a personal trainer where a gym says Members / Trainer (spec Part 3 §2.2).
     One table in `@app/shared`, read by the API and by every screen, so the server's
     refusals and the screen agree. *Merged 2026-09-08 (PR #55).*

3. **Onboarding v2, server side** (split 2026-09-08; the science first because every screen shows its number).
   - 3a. [x] **The plan maths and its sanity rules.** Resting burn, daily activity + training days, pace → deficit or surplus, macros, finish date; target direction, healthy-weight floor, over-a-year pace, the calorie floor; the under-18 and flagged-condition rules (no deficit). Pure functions, a test for every rule. Nothing on screen yet; the click-through says so.
   - 3b. [x] **Health screening, Safe mode and the consent log, server side.** ONE general question stored as a yes/no plus the "Check first" choice, changeable any time (RULINGS 2026-09-09 — no named condition, ever); Safe mode and "no calorie cut" derived on the server and read by the plan maths (`hasCondition` + `safeMode`, reasons `health_answer` / `safe_mode`) and by the live macro rings; the consent log (time, app version, wording verbatim) for sign-up, the health step and the plan screen. Tenancy test on every route. *Built 2026-09-09, branch `health-screening`; review round 1 fixed the same day; Kd ruled on the consent log (kept six years, sweep built) and the old conditions box (off at 4b); re-check done the same day (one High — the consent limiter throttled a whole gym — five Low, all fixed); round-2 re-check found no Critical/High (four Low + three test gaps, all fixed). No click-through: 3b has no screen (RULINGS 2026-09-09). Merged 2026-09-09 (PR #57, CI green, branch deleted).*
   - 3c. ~~Allergen tags on every food.~~ **Struck 2026-09-09** (RULINGS): no tags, no allergy question; every meal suggestion carries a food-allergy caution line instead, built with 7a. (Contraindication tags on exercises were struck the same way: with one general health question the app cannot know which exercises to exclude, RULINGS 2026-09-09; a cleared person gets the normal plan with a "follow your professional" line, built with 6a.)
4. **Onboarding v2, screens** (split 2026-09-08 along the twelve screens in RULINGS; master is not live, so a half-built flow may land and the next half continues it).
   - 4a-i. [x] **The answers and the live plan, server side** (split 2026-09-09: seven screens plus a migration, two routes and the macro rings' move is more than one chat and more than a reviewable diff). Stores what the screens ask — the one main goal, pace, the day, the two fitness checks, the week, equipment — saved as you go, so a screen never wipes an answer it did not ask about. `GET`/`PATCH /v1/users/me/onboarding` answer with the plan numbers or the honest list of what is still missing; "today" comes from the device's time zone and the server clock, never a body field. Nothing on screen yet, so no click-through (RULINGS 2026-09-09). *Built 2026-09-09, branch `onboarding-plan-server`; six review rounds, the last with no Critical/High and its re-check waived by Kd. Merged 2026-09-10 (PR #58, CI green, branch deleted).*
   - 4a-i-b. [x] **Weight has one source and no copy** (Kd's redesign, 2026-09-10: the profile form echoing a weigh-in's number left a "typed by me" entry the person never typed, so deleting the mistaken weigh-in kept its number). Migration `0027` drops the `users.weight_kg` cache after re-running 0026's safety backfill; every reader computes the weight from the history. No screen changes, so no click-through (RULINGS 2026-09-09). *Built 2026-09-10; Kd waived the re-check (no Critical/High). Merged 2026-09-10 (PR #59, CI green, branch deleted).*
   - 4a-ii. [x] **Screens 1–7:** goal · about you · target · your day · your training (push-up and plank checks) · your week · equipment. Tap-only, saved as you go on the 4a-i routes, the live plan number on every screen from the moment it exists (after "your week", screen 6, when the eight core answers are in; before that no number box at all, RULINGS 2026-09-10 — no number from a default, RULINGS 2026-07-15). They replace the old five-step form; its free-text conditions box stays in Settings until 4b switches it off (RULINGS 2026-09-09). Screen 7 makes "no equipment" exclusive as you tap, AND drops "none" as it LOADS an answer the old form already wrote: the v2 contract refuses "none" beside real equipment, the old free multi-select can still send that pair, so without this a person carried over from it taps Save without touching equipment and gets a 400 with nothing on screen to explain it (the old form is left alone until this screen replaces it). Also here: `onboardingCompleted` is stored exactly as the client sends it, so `true` with no answers saved opens the training side (the v1 PUT has always been the same, and nothing on the server reads that column as a gate) — the server refuses `true` while `missing` is non-empty, which is what RULINGS 2026-07-19 asks for. The v1 PUT, which only Settings writes once these screens replace the old form, still stores the flag as sent. *Split 2026-09-10: the macro rings' move is 4a-iii. Built 2026-09-10 on branch `onboarding-screens-1-7` (PR #60); Kd's click-through passed and his look changes landed the same day (RULINGS 2026-09-10). The five fixes from his second look were built the same day (RULINGS 2026-09-10): flick wheels, the name first on "About you", plain push-up and plank questions with "Not sure", "How is this worked out?" from the server's own steps, two drawn icons. With them, the plan's daily burn became the exact sum of the whole-kcal steps it prints, and the finish date is counted in whole hundredths of a kilo (a floating-point crumb had put some dates a day late). Kd's second click-through found a target on the wrong side of the weight accepted (fixed the same day, RULINGS 2026-09-11). Review round 1 (2026-09-11): 3 High, 6 Low, all fixed — the wheels no longer save a number the person did not pick, and the plan's working must multiply out as well as add up. The re-check the same day found no Critical/High and four Lows, all fixed: a slow drag saves where the finger lifts, and every figure the plan panel prints twice must agree. Kd's click-through of the wheels passed (RULINGS 2026-09-11: tap a wheel before flicking it, kept). Merged 2026-09-11 (PR #60, CI green, branch deleted).*
   - 4a-iii. [x] **One daily-calories number: the macro rings read the plan.** The nutrition targets route (`/v1/nutrition/targets`, shown by the web's macro rings) moves onto the stored plan answers through the one function the onboarding routes use — before, it read the old profile with a fixed −400/+300 and could differ from the number screens 6–7 show by a few hundred kcal. Someone who finished the old form has no one goal and no "your day", so the rings' honest empty state names the questions still open and "Answer now" opens onboarding on the first of them; a person who already finished also gets "Back to the app" there, beside "Sign out", and a name changed on the way (or in Settings) reaches the sidebar and the dashboard's greeting at once. Protein (Kd asked for the science, RULINGS 2026-09-11): grams per kilo, never a share of the calories, counted at the BMI-30 weight for a heavier body, each figure's source named in "How is this worked out?". Settings' goal chips still show the `fitness_goals` list, so until 4a-iv both save routes keep it and the one goal in step, and a change takes effect at once (RULINGS 2026-09-11): ticking Weight Loss or Muscle Gain unticks the other; on a Settings save the ticked one sets the calories, else the main goal stays while it is ticked, else the first goal ticked; screen 1's goal goes first in the list and only a goal that fights it leaves. A target left on the wrong side of the weight (the goal changed, or the target reached) makes the rings ask for a new one, never call it unanswered nor show the held weight as the goal's number, and Settings' target box refuses one typed in, judged by the goal the calories follow (RULINGS 2026-09-11). Comes BEFORE 4a-iv: the rings must read the plan before the goals move underneath them. *Built 2026-09-11 on branch `rings-read-the-plan` (PR #61). Kd's click-through passed; merged 2026-09-11 (PR #61, CI green, branch deleted).*
   - 4a-iv. [x] **Many goals, and a gym.** Screen 1 becomes ONE weight choice (lose · keep · gain — it alone sets the number) plus "also work on", any number (RULINGS 2026-09-10). A `weight_goal` column with its CHECK replaces `main_goal` by migration, backfilled from today's goal (weight loss → lose, the goals that keep the weight → keep; the people who chose build muscle are asked, never given one: Kd's answer, RULINGS 2026-09-11); the old goal joins the "also" list unless it was weight loss. `fitness_goals` becomes the "also" list, its enum losing weight loss and gaining get stronger and better balance; the v1 profile PUT and Settings' goal chips take the same two questions, which ends the goal list 4a-iii keeps in step with the one goal from both sides; Settings' "Reset onboarding" then wipes the v2 answers too (pace, your day, push-ups, plank: RULINGS 2026-07-20 — today its `PUT {}` keeps them while its confirm box promises a blank wizard); `PLAN_GOAL_BY_MAIN_GOAL` goes, the direction is stored, never derived. "Build muscle" ticked raises protein to at least 2.0 g per kg; nothing else in the calorie maths reads the "also" list. Screen 7 gains "A gym (everything there)" beside the home equipment, a new equipment value with its CHECK (Kd said yes, RULINGS 2026-09-10, decision B). *Built 2026-09-11 on branch `many-goals-and-a-gym`; at Kd's click-through screen 1 became one grid three to a row under "Your goal", a ninth goal Stay healthy joined, and "A gym" lost "(everything there)" (RULINGS 2026-09-11). Migration `0028` (the weight choice with its CHECK; the goals and the equipment lists CHECKed, "no equipment" alone; `main_goal` dropped); Build muscle ticked sets protein to 2.2 g per kg, Morton 2018's figure (the line above said 2.0); Settings asks screen 1's two questions and screen 7's equipment with the screens' own line icons; "Reset onboarding" is `DELETE /v1/users/me/onboarding`, which clears every answer on the profile, the ones only the screens ask included, and keeps the name, the weigh-ins and, until 4b, the health answer. Kd's click-through passed the same day. Merged 2026-09-12 (PR #62, CI green, branch deleted).*
   - 4b. **Screens 8–11** (split 2026-09-12: four screens, a Settings screen, the running columns and two migrations is more than one chat and more than a reviewable diff).
     - 4b-i. [x] **Screen 8, health — the ONE question**, on the 3b routes: the yes/no, "Check first" (cleared · not yet) and the disclaimer tap. The question's words gain "or take any medicine, including for weight loss" (Kd's decision C, RULINGS 2026-09-10). Settings gets the same question, in the SAME component, so the answer can be changed later and everything follows at once (RULINGS 2026-09-09). Finishing setup is refused while it is unanswered (`missing: ["health"]`), which is why Settings' "Reset onboarding" now clears the health answer too — the question is back in the wizard, so a cleared yes has a screen to be given again on. The old free-text "medical conditions" box (Settings) goes with its untrue hint "Helps the AI give safer advice" (RULINGS 2026-08-18), and `user_fitness_profiles.medical_conditions` is DROPPED with everything typed in it by migration `0029` (RULINGS 2026-09-09). The three disclaimer wordings in `packages/shared/src/health.ts` gain a v2 that keeps "follow their advice" and loses the comparison with the app, so the consent log keeps what each person agreed to (RULINGS 2026-09-10, decision D). *Built 2026-09-12 on branch `health-question`; Kd's click-through passed the same day (*"smoke passed"*). Three review rounds, each finding less: round 1's High was the note under the question promising the app keeps less than it keeps; the first re-check's High was a finish the server refuses leaving a stale answer on screen, which the tap could not replace; the last round found no Critical/High and three Lows, fixed here — no health answer is taken while a finish is out (a refusal would forget one the server had just stored), the note promises no detail AT ALL rather than naming the condition alone (the question also asks about medicine, an injury and pregnancy), and the unanswered screening is one shared constant that crosses the contract, read by the server and both screens instead of hand-written in each. Two tests that could not fail were fixed with it: the build's own `VITE_APP_VERSION` now goes through the clip (the rule was tested, the wiring was not), and "the health question is above the fitness form" now pins the whole form, not just its Save button. Merged 2026-09-12 on Kd's word — *"fix and merge"* (PR #63).*
     - 4b-ii. [x] **Screen 9, food, and screen 11, the gym code.** (A code that arrives on a poster link is applied first: 4b-ii-b.) Food asks the diet — vegetarian · vegetarian with eggs · non-vegetarian · vegan (RULINGS 2026-09-10) — and meals a day. NO cuisine question and no cuisine data (RULINGS 2026-09-12): the food table holds 131 foods with no cuisine label on any of them, and suggestions (7a) read the whole table by diet and the calories left. *Built 2026-09-12 on branch `food-and-gym-code`. Migration `0030` adds `diet` and `meals_per_day` with their CHECKs, both NULL for everyone (nobody was ever asked, and a guessed diet would put meat in front of a vegetarian). Both answers gate the finish, in the shared enum beside `health`; the gym code never can, because most people have no code. Screen 11 draws the join door's own two components, the ones Settings' Gym tab draws, so three places cannot answer differently — its "Back to your dashboard" is hidden mid-setup, where `ProtectedRoute` makes it a loop. Finish moved to the last screen with them, so the disclaimer tap on screen 8 is now named and reachable from there ("Read the note on the Health screen and tick it" + Go to Health), and Back is held while a finish is out, as the step bar already was. Settings' Fitness tab asks both questions, in the screen's own words and icons. Changed 2026-09-13: the profile PUT refuses to finish setup while an answer is open, as the onboarding PATCH does, and keeps a finish already stored, so Settings still saves; the meal ideas after a workout follow the diet (Kd, RULINGS 2026-08-06 as amended) and carry the food-allergy caution; screen 11's "Try again" goes to the code box below instead of back into setup; Settings offers "Not set" for meals only while none is stored. Kd's click-through of the fixes passed 2026-09-13 ("all passed"). Changed on this branch from that click-through (ce4420e, 2026-09-13; RULINGS 2026-08-06 as amended 2026-09-13): the meal ideas' words in `apps/api/src/modules/workouts/summaryContent.ts` and their tests — over 400 kcal: "Protein shake + banana" (Within 30 mins; vegan "Plant protein shake + banana") · "Grilled chicken + potatoes + vegetables" (Within 2 hours; vegetarian and below "Grilled tofu + potatoes + vegetables") · "Greek yogurt with berries" (**In 3–4 hours** — never "before bed", nor "Later today", which does not count from the workout (Kd, RULINGS 2026-09-13); vegan "Soy yogurt with berries"); over 200: "Protein shake or chocolate milk" (Within 30 mins; vegan "Plant protein shake or soy chocolate milk") · "Eggs + whole grain toast + avocado" (Within 2 hours; vegetarian and vegan "Tofu scramble + whole grain toast + avocado"); the rest: "Banana + peanut butter" (Within 30 mins, every diet) · "Light salad with grilled protein" (Within 2 hours; vegetarian and below "Light salad with chickpeas"). Same bands, same thresholds; the unit test's "verbatim port" claim becomes "the market's words, RULINGS 2026-09-13". Kd's click-through of the last fixes passed 2026-09-13 ("all passed"); merged the same day (PR #64).*
     - 4b-ii-b. [x] **A poster's code survives sign-in and setup, and is applied first** (split from 4b-ii, 2026-09-13, because it goes through the sign-in door). On 2026-09-07 Kd agreed that "the gym code is applied first" means *"so they are joined even if they stop halfway"* (RULINGS 2026-07-19, amended that day). Today `/org/join?code=…` loses the code twice: signed out, the login page lands the person where it decides and never returns to the address; signed in but not finished, `ProtectedRoute` sends them to setup without it. Carry the code through both, and put it in front of the person before screen 1 — filled in, never sent without their tap (a poster must not enrol whoever scans it). The plan asks Kd what that first step looks like. *Built 2026-09-13 on branch `poster-code`. Kd picked "Your code" moved to the front of setup (RULINGS 2026-09-13). The code is kept in the tab as the door is (`CarryJoinCode` on the join address; Google leaves the site and comes back), and only a code the server could have made is kept (`linkedJoinCodeSchema` in `@app/shared`), so an edited link cannot put its own words on the sign-in page. Signing in lands a set-up person on the join page with it and anyone else in setup, where Continue from the code goes on to the first open question. The kept code is forgotten when sent, on reaching the join page, when a newer link carries no code, and on sign-out; a code never sent goes with Finish to the join page, still unsent (Kd, RULINGS 2026-09-13, from the review). No change in what the server does. Kd's click-through passed 2026-09-13 (*"all passed"*), and again for the Finish change the same day (*"all passed"*). Merged 2026-09-13 (PR #65, CI green, branch deleted).*
     - 4b-iii. ~~Running, asked and never tested.~~ **Moved out of setup to 7b, 2026-09-13** (Kd, RULINGS 2026-09-13: setup asks nothing about running, as Strava's sign-up does). Its questions go with it, asked when someone opens the running feature: "How long can you jog without stopping?" — I don't jog · under 5 minutes · 5 to 15 · 15 to 30 · over 30 — tap-only, "Not sure" allowed, stored as a band; "Do you run, or want to start?"; and, for "I run", the runner's own: days a week now · usual weekly distance · longest run in the last month · a recent time over a known distance, optional (5K · 10K · half · marathon).
   - 4c. [x] **Screen 12, your plan:** live numbers, the disclaimer, Adjust, and the Safe mode version that shows no workout or run plan. *Built 2026-09-13 on branch `plan-screen` (PR #66): the last screen, after "Your code", with the week of workouts (Safe mode's words in its place), every answer with an Adjust, and its own disclaimer tap, which Finish waits for. Kd's muscle ruling (RULINGS 2026-09-13) is built with it; no migration, no new route. The review and its re-check found no Critical/High; the re-check's fixes were not re-checked, on Kd's word. Kd's click-through passed 2026-09-14 (*"all test passed"*). Merged 2026-09-14 (PR #66, CI green, branch deleted).*
   - 4c-ii. [x] **A target under the healthy weight is said on "Your target", as it is picked** (Kd, 2026-09-14, from 4c's click-through; RULINGS the same day). *Built 2026-09-14 (PR #69).* The moment the target wheel lands under the lowest healthy weight for the height, a panel under it says so, and what the plan does: it will not take the person below that weight, or, for someone already there, will not lower their weight. The target can still be picked, the number box keeps its line, and Settings' target box says the same. At 16 and 17 the figure is the teen one (Kd's answer) and the words say "for your height and age". Each screen says it only where the target reads under that weight as that screen shows it, so a target in pounds that reads as the healthy weight is never called under it. Kd's click-through passed 2026-09-14 (*"all test passed"*). The review found no Critical/High; its four Lows and three weak tests were fixed the same day, and the fixes were not re-checked, on Kd's word (*"merge"*). Merged 2026-09-14 (PR #69, CI green, branch deleted).
   - 4d. [x] **The sign-up disclaimer tap** (found 2026-09-13 while building 4c). RULINGS 2026-09-07 asks for one explicit tap at sign-up, stored with the time, the build and the words, as the health step's (4b-i) and the plan screen's (4c) are. The server has held the `sign_up` wording (v2) and the consent route since 3b, but no screen shows the words and nothing records the tap. The plan asks Kd where it sits (a Google sign-in leaves the site, and the tap can only be stored once the account exists). *Built 2026-09-14 (PR #70).* Kd picked a screen of its own straight after signing in (RULINGS 2026-09-14): "Before you start", once per account, on the training side (before setup and the member app); the console skips it, as it skips setup, so a person who runs an organisation meets it on coming to train (Kd, from his click-through, which passed the same day, and so did his click-through of that change: *"all passed"*). The profile says whether this account has ticked the note in today's words, read from the consent log (this person, this note, these words), and the web's guard draws the note in place of the screen asked for, so a poster link's address survives it, on every route but the six console routes, which opt out as they opt out of setup; a profile that cannot be read shows the note rather than letting someone in unrecorded. The tick is setup's own, now shared. The note's words are Kd's own, the sign-up wording's v3 (RULINGS 2026-09-14). No migration, no new route.
5. **Goals and the Today screen** (split 2026-09-08).
   - 5a. [ ] **Goals, server side.** The goal object (type, target, period day/week/month/year, app-set or user-set), the starter set from onboarding answers, progress from existing data. Routes with the stranger-gets-404 test.
   - 5b. [ ] **The Today screen.** Rings from progress, the goal list, change, remove and add. The rings say why a plan holds no calorie cut (under 18, a yes on the health question, Safe mode: the plan's `no_deficit` reasons); the setup screens say it since 4a-ii, and the Nutrition page's rings still do not.
6. **Recommended workout and the weekly plan** (split 2026-09-08).
   - 6a. [ ] **The plan builder, server side.** Shape from days and level; exercises by goal, equipment and health limits; numbers by level and goal; push-up and plank checks set the starting numbers (skipped = the self-rating alone sets them); progression to the harder variant once mastered; Safe mode builds no plan. Pure and tested. Several goals (RULINGS 2026-09-10): each "also work on" goal adds its own kind of work — build muscle and get stronger: lower reps and progressions · get fitter and endurance: circuits and intervals · flexibility: mobility blocks · posture: back and core · better balance: single-leg work · stress relief: slower sessions with a breathing cool-down · stay healthy (Kd added it at 4a-iv's click-through): the WHO's weekly activity guidance (WHO, 2020), checked against its own text when 6a is built; the week's sessions are shared out across the ticked goals and the plan screen says how much each gets; with more goals than sessions, goals share a session. The calorie number never reads the "also" list.
   - 6b. [ ] **The weekly plan screen.** The week, swap, move, add. Adds the **Exercise** option to the dashboard's Recommended box (7a-ii): today's session from the week's plan, and for a person in Safe mode why there is none. (The dead old-backend "Recommended for you" box itself is replaced at 7a-ii.)
   - 6c. [ ] **Doing a planned workout.** Camera counting where the engine supports the exercise, guided otherwise; a finished workout feeds progression.
7. **Meal suggestions and the run plan** (split 2026-09-08; 7a-i and 7c added 2026-09-12 on Kd's asks; 7a-ii added 2026-09-13). Order (RULINGS 2026-09-12): after the setup screens come 7a-i, then 7a and 7a-ii, then 7c — 7c swaps meals out of 7a's suggestion, so 7a must exist first; 5, 6 and 7b follow in their numbers.
   - 7a-i. [x] **A bigger food list, and every food's diet** (Kd, 2026-09-12). The curated table is code, not a table: `apps/api/src/modules/nutrition/foods.ts`, 131 foods (counted 2026-09-12), each with its calories and macros per 100 g. It grows to about 300 — vegetables, fruits, pulses, whole grains, dairy, fish, eggs, nuts and the everyday dishes people actually log — LEADING with meat, fish and eggs, because the market is the US, Canada and Europe (RULINGS 2026-09-12), with every number's source named. Every food gains its diet level (vegan · vegetarian · with eggs · non-vegetarian), which is what makes screen 9's answer mean anything: without it a diet can be asked and nothing can be filtered by it. It also narrows the meal scanner's honest gap — an item our list does not know is dropped from the totals and named ("Couldn't identify: …"), and a bigger list is the only thing that closes it. Comes BEFORE 7a. *Built 2026-09-14 on branch `bigger-food-list` (Kd: "go", RULINGS 2026-09-14). 318 foods: the 131 kept under their canonicals (renamed where a name must say what its numbers are, e.g. "Caesar salad (with chicken, no dressing)", with Caesar dressing its own food, or carry an Indian dish's English name) and 187 new, the most of them meat, fish and eggs (44), with a homemade and a store-bought roti (Kd, RULINGS 2026-09-14). Every food cites its table entry — USDA SR Legacy 241, USDA FNDDS 72, UK CoFID 5 — and 55 of the old foods' kcal per 100 g changed to their table's (an egg, an egg white and an idli had one piece's figures as 100 g's; a bowl of pho counted 1,400 kcal). Every food carries its diet (`diet`, the lowest rung that eats it; `dietAllows` in `@app/shared` is the ladder, which the post-workout ideas now read too). A photo's count multiplies a food's own serving only where that serving is one piece or a sealed pack (six nuggets are six nuggets; ten grapes stay one 100 g serving). The matcher finds a food by its canonical first, then by whole words: on master a search pick of peanut butter, almond butter, orange juice, apple juice, pineapple, sweet potato or butter chicken was priced as butter, orange, apple or potato, and the scan logged "tea" as sirloin steak and "water" as tuna. `tools/check-food-sources.ts` re-reads the three tables and compares every number. No migration, no new route; the search response is unchanged but for fibre, which may be null. Kd's click-through passed 2026-09-14 (*"all the tests passed"*); from it, a packaged product from Open Food Facts reads with its brand and "Packaged product · Open Food Facts" (RULINGS 2026-09-14), and his click-through of that passed too (*"as fo rthe test all passed"*); the Open Database License's notice under the list was added after it. From the review's re-check the same day: a photo's count never multiplies a cut ("banana slices"), another vessel, or a can, bottle or pot shown in a dish the person saved; an item no food matches is named under "Couldn't identify"; the photo sheet's stepper starts at the scan's count, one piece a step; and two versions of one food are never told apart by how many words their brackets hold, so any order of roti, chapati and flatbread is the homemade roti. From the re-check of those fixes (2026-09-15): a photo's count is read by the unit of the food's serving, never by one example's words — pieces times the piece whatever they sit in ("pieces" counts whole things, and is a cut only of a fruit, a vegetable or an egg), cans, pots, glasses, cups and bowls times one of what the photo shows them in (a saved dish, a known container, else the serving), and no count for a serving by weight or spoonful, with every unit on the list given its rule in a test; a packaged product prices a scanned item only where the product's name holds every word the photo gave the item; and what matches no food reads "Not in our food list". From that re-check (2026-09-15): a count is first read for what it counts, whatever the food's unit — bits cut from a food are never counted, "pieces" are whole only of nuggets, meatballs, slices, a label's own piece and Appendix B's small Indian breads and snacks, and a vessel named before "of" weighs as that container ("mugs of coffee") — and a packaged product is served by the pack and grams its label names ("1 bottle (65 ml)"), which also ends the first-number serving ("1 oz (28 g)" was 1 g); a product's name must hold a noise word that is the food's own ("glass noodles"), accents are folded; the sheet reads "Not in the total", and points at "Add an ingredient" only while it is there; cola is served by USDA's 370 g can; a count over 30, or one weighing past what one item may, is not used; and a container or size the photo names like an object's built-in property ("constructor") no longer blanks or crashes the scan. Its re-check, the third (2026-09-15), found two more Highs and four Lows, and Kd ruled the portion redesign (RULINGS 2026-09-15; 7a-iv). Fixed here before merging, the reviewer's way (2026-09-15): H1, a poured serving is no longer shrunk by how full its container looks — fill scales a serving only where the serving is all its container holds (cup, half cup, can, bottle, container, pot, tub, and a label's bag, box, carton, jar, pack, package, packet, pouch, sachet: `WHOLE_VOLUME_UNITS`), and a glass, bowl, small or portion serving keeps its weight (red wine in a glass at 0.4 was 60 g, is 150 again); H2, a plural before "of" that is no cut and no "pieces" is what the count counts, whatever cut or pieces fill it — "bowls of beef stew chunks" ×2 is two bowls, 510 g, and "large bowls of dal" ×2 550 g — where the photo shows the food in nothing or in a container of that name ("scoops of ice cream" in a cup are counted as ice cream in a cup); one weighs what the rungs know it holds (a known container, a saved dish, the food's own serving, and "servings", "portions" and "helpings" are the food's serving) or, for a food served by a vessel, the serving, and otherwise the count is not used and the food's pieces are never counted ("plates of chicken nuggets" ×2 is no two nuggets); a count beside one container ("a plate of nuggets", six) stays a count of what it holds; L2, the 10 kg cap checks the top of the range, not the middle; L4, a count that is not a whole number from 1 to 30 (0, 2.5, "3"), or is left out, reads as unknown instead of rejecting a paid scan. L1 (a pack word after a brand loses the product: "monster energy can") and L3 (a hinted cup reaches the water cup: "a cup of almonds" 240 g) are 7a-iv's, with the resolver they belong to. That re-check (2026-09-15) passed H1, L2 and L4 and found two Highs in H2's fix, fixed here: a plural counts the photo's container only where it is what the container is, the last word of its name ("servings" in a serving bowl are no bowls: six servings of nuggets are 96 g, not 2,100); and where the photo shows none, only a plural that names a container counts containers — what each Appendix B container is, the vessel and pack units, a plate, a tray, the serving words, or a saved dish — while any other ("stacks of roti", "piles of nuggets") reads as its singular (eight rotis, 320 g, not one). A container is the words of its name that name a container, wherever they stand ("bowl with lid", "plate with rice" and "cup and saucer" are a bowl, a plate and a cup, so their plurals count them), never a serving word where another does ("bowl (1 serving)" is a bowl: six servings of nuggets in it are 96 g), and a name with none is its last word ("steel flask" is a flask, and "serving" a serving: three servings of rice in it are 300 g). Merged 2026-09-15 (PR #71, CI green, branch deleted).*
   - 7a-iii-a. [x] **The whole USDA food list, in the food search** *(built 2026-09-16 on branch `usda-food-table`; search order ruled that day — our list, then USDA, then packaged products. Migration `0031`; `tools/import-usda.ts` run once per environment, RUNBOOK `load-usda-food-table.md`; 13,225 foods and 31,169 household measures loaded and counted by the tool. Amended the same day, before merging: within the USDA rung the FOOD ITSELF ranks above the dishes made from it — what was typed against the head of the description, exactly, then as a start, then its first word — because "pumpkin" answered Muffin, Bread, Cookie, Pie and Pancakes before the vegetable; the last three places of the box are held for a packaged product, because 13,225 USDA foods filled every place a generic word had and left a jar nowhere to go; a measure's name is cut at a space instead of at the fortieth character; each release is checked on every run, cache included, against the SHA-256 this tool was built against (USDA publishes none), and a download that fails it is never cached; and the importer takes `--database-url-env=NAME` so no production password is typed on a command line. From the review the same day: accents are folded on both sides of the search, the stored `search_text` and the typed query alike, so "jalapeño" finds USDA's "Jalapeno"; a search waits at most 3 s for packaged products; and packaged products never take more than half of the places left. Merged 2026-09-16 on Kd's word (PR #73, CI green, branch deleted).)* (split from 7a-iii on 2026-09-16 — the table and the scanner together are more than one reviewable diff; sizing is the chat's, RULINGS 2026-09-08; Kd: *"go"*, RULINGS 2026-09-16). Built BEFORE 7a-i-b and 7a, from master, its own chat and pull request. A new table holds every food of the two USDA FoodData Central releases the food checker already reads — SR Legacy 2018-04 (7,793 foods) and FNDDS 2024-10-31 (5,432), 13,225 together (counted 2026-09-16; the tool prints its own count) — public domain, CC0 1.0 (RULINGS 2026-09-16). Per food, per 100 g: kcal, protein, carbohydrate, fat, fibre, total sugars, saturated, monounsaturated and polyunsaturated fat, cholesterol, sodium, potassium, calcium, iron, magnesium, vitamin D and vitamin C — SR Legacy's `food_nutrient.csv` names a nutrient by its id (1008, 1003, 1005, 1004, 1079, 2000, 1258, 1292, 1293, 1253, 1093, 1092, 1087, 1089, 1090, 1114, 1162), FNDDS's by its number (208, 203, 205, 204, 291, 269, 606, 645, 646, 601, 307, 306, 301, 303, 304, 328, 401); a figure the table lacks is null — and its household measures from `food_portion.csv` (amount, unit, SR's `modifier` or FNDDS's `portion_description`, gram weight), which 7a-iv reads for piece and cup weights. Migration `0031`: `usda_foods` (fdc_id, release with a CHECK, description, its first word for 7a-iii-b's matching, the nutrients, a generated English `tsvector` with a GIN index) and `usda_food_portions`; public data, no owner, no tenancy. `apps/api/tools/import-usda.ts` downloads the two zips into the checker's cache (the unzip and CSV readers move out of `check-food-sources.ts` into a file both import) and upserts by fdc_id in one transaction, so a second run changes nothing; it takes the database address as its own argument and never reads `apps/api/.env` (Kd's Neon data); tests give the readers small CSV strings and the upsert a handful of rows, never the download. RUNBOOK: run it once per environment after migrating (Stage 4 item 1). A USDA food's canonical is `usda_sr_<id>` or `usda_fndds_<id>`; its serving is its first household measure with a gram weight ("1 cup · 158 g"), else 100 g; it carries no diet label, so 7a never suggests it and screen 9's filter never sees it. The search box finds USDA foods after our list and before Open Food Facts: every typed word must match (a prefix on the last), FNDDS before SR, the fewest words first; a USDA food reads with a grey line "USDA FoodData Central", as a packaged product reads its own (RULINGS 2026-09-14), and the citation USDA asks for goes under the list beside the Open Food Facts notice. `nutritionSourceSchema` gains `usda`; a saved meal's items resolve by canonical as today, so a USDA food is edited and re-priced like any other. Tests: the readers on small CSV strings (a nutrient missing → null; SR's ids and FNDDS's numbers), the upsert run twice, the search rule as a table test on fixture rows (every word · prefix · FNDDS first · fewest words), the route test (a USDA food searched, added by grams, saved, its numbers the table's × grams ÷ 100), the cross-user test on the meal. Click-through: search "cappuccino", add it, see its line and the citation. No model call, no new dependency.
   - 7a-iii-b. [x] **The scanner prices every food it sees** (Kd, 2026-09-15; RULINGS 2026-08-24 as amended that day; the plan *"go"*, RULINGS 2026-09-16). After 7a-iii-a, from master, its own chat and pull request. Today the model names foods and the app keeps only those our 318-food list holds — a plate of two topped toasts came back as "Avocado", "Sandwich (turkey)" and a Nescafé sachet, while the same model, asked plainly, listed all fourteen foods with grams and calories. (1) The reply: one compact list per food — `[name, generic name, vessel, fill 0–1, size, whole-piece count, grams, kcal, protein g, carbs g, fat g]`, `null` in a slot the model cannot fill — beside `meal_name`, `unknown_items` (what it could not name) and `photo_quality`. The vessel is one of 7a-iv's fixed list (katori, bowl, large bowl, thali section, tumbler, chai cup, cup, mug, glass, can, bottle, pot, tablespoon, teaspoon, plate, none; a word off it reads as null); the count is of WHOLE pieces only, never slices, chunks or pieces cut from a bigger item, and the prompt says so; the five estimates are always filled — this is 7a-iv's form, written once. Every plate-reading instruction of today's prompt stays word for word; "Never output calories…" goes. The reply is parsed in `@app/shared` (`mealVision.ts`) as a tuple, and one that breaks it is "unreadable" as today; Gemini refuses a response schema for this contract (400), so JSON mode and the prompt carry it. Fallback, only if the shape check shows the model cannot hold a list: short keys (`n, h, v, f, s, c, g, k, p, cb, ft`), estimated 253 tokens on the biggest plate against 275 today — never the old names back. (2) Matching, in this order, table-tested: our list by whole words (`findCurated`) → USDA exact description, case and accents folded → USDA every-word: every word of the generic name is in the description and the description's first word is one of the generic name's words, both stemmed by the same dictionary; FNDDS before SR, then the fewest words → a packaged product whose name holds every word (as today) → the model's estimate. One cross-check on every table match after the first rung: the table's kcal per 100 g against the model's own (kcal ÷ grams × 100) — more than 3× apart either way is a wrong food ("juice" → a concentrate) and gives way to the estimate. Nothing is dropped and nothing renamed; the matched name is shown. (3) An estimate row: source `estimate`, canonical `est_<slug>`, per-100 g figures from the model's numbers, its portion the model's grams; a per-100 g kcal over 900, or macros whose energy (4 × protein + 4 × carbs + 9 × fat) is outside 0.7–1.3 × its kcal, is no number. A food the model named with no usable number that nothing matched stays out of the total and is named under "Not in the total" — `unknownItems` keeps only that and the model's own unknowns. The per-100 g figures ride with the item in the meal's `items`, so an edit after saving rescales without a lookup; `nutritionSourceSchema` gains `estimate`; a matched food's portion is resolved as today until 7a-iv. (4) The sheet: every food listed, each row with its name, its portion (an estimate's grams read "~120 g · estimate"), kcal, protein, carbs, fat, a source tag — USDA · Our list · Packaged product · Estimate — and "Change", which opens the search "Add an ingredient" uses and swaps the row's food at the same grams (sent as an added item; no correction pair); totals read "about" when any row is an estimate, and so does the day's line under the rings; the stepper and grams as today. (5) Cost: `apps/api/tools/measure-scan-cost.ts` runs a folder of photos through the real provider and prints each photo's tokens and cost and the average, the key read from the `GEMINI_API_KEY` line of a `--env` file and never printed; no photo and no key ever in the repository. Kd's eight plates (768 px) and the 2026-09-15 baseline (`results.json`: 211 output tokens and $0.00068 a scan on average, the biggest plate 301) live in that chat's scratchpad (HANDOFF 2026-09-16) and are copied to `D:\Projects\ai-home-gym-plates\` first. Calls: ONE shape check on the biggest plate ("download (3)" — the records named (2), but (3) wrote the most on 2026-09-15, 336 tokens to 301), a second only if the model breaks the form, then the eight — at most ten, each one's tokens in HANDOFF; never a sweep (RULINGS 2026-09-15). An average above $0.00086 a scan stops the card: the number goes to Kd (raise the $379 band · accept it · cut fields) before anything merges. (6) Tests: every matching case in a table test (each rung; FNDDS over SR; the fewest words; the first-word refusal; the 3× cross-check both ways; the bounds; an estimate row; a named food with no number), the route test with a food the table lacks (an estimate row on the sheet, "about" totals, the stepper rescaling it, saved, its grams patched later), the cross-user test, the eight plates' replies as fixtures (`apps/api/test/fixtures/plates/`, the reply text only) run through the resolve so a change in the rule shows as a diff, and web tests for the tags, "about" and Change. Temperature: Google's default (RULINGS 2026-09-16). Portion accuracy (reference objects, the person's dishes, the phone's depth sensor) is a later card. Click-through: the toast plate. *Built 2026-09-16 on branch `scanner-prices-every-food`. Measured with 9 Gemini calls of the 10: the shape check held the list, but wrote "150.0" for every whole number, so the prompt asks for numbers that "never end in .0"; the eight plates then averaged $0.000802 a scan (612 tokens in, 247.5 out; $0.00068 before), under the gate — each call in HANDOFF 2026-09-16. On the loaded USDA table the eight plates' 44 foods are all on the sheet: 35 from a table (33 our list, 2 USDA), 9 estimates, 2 of them USDA's brewed iced coffee at 1 kcal per 100 g overruled. Settled while building: the 3× check runs on every table's match, our list's too, as RULINGS 2026-09-16 words it (the line above said after the first rung), and only where the two differ by more than 10 kcal per 100 g, so black coffee at 1 against 4 is not another food; an estimate's macros may miss its kcal by 30 % or by 10 kcal, the rounding of whole grams; USDA's scan rung takes the whole description, then its head (7a-iii-a's "food itself" rule, so "banana" is "Banana, raw" and never "Banana split"), then every word; the vessel reaches today's resolver by Appendix B's name (a katori is the standard katori, a tumbler the steel tumbler); a row's Change has Undo; the sheet credits USDA and Open Food Facts as the search box does. Known and left for later cards: our list's foods keep the old portion rule (bacon 100 g where the model saw 30 g — 7a-iv; Kd, RULINGS 2026-09-16); USDA's answer for a bare word our list lacks can still be a poor one where its calories agree ("egg" is "Egg, creamed"), which our list's words cover today; an alcoholic drink no table has fails the macro-energy check (its kcal come from alcohol) and reads "Not in the total". From the review (2026-09-16; 2 High, 6 Low, 4 weak tests), fixed here: a USDA food is served at the grams the model saw, USDA's measure once where it gave none (the measure times the count read two pumpkin pieces as 460 g); among USDA entries of one name, every entry within the model's own error (30 % or 10 kcal) of what it saw, and every entry within 10 kcal of the nearest one, is as near, the plainest of them is the food, and never an entry of a lower name step ("radishes" at 16 is "Radishes, raw", not "Radishes, pickled"; pumpkin at 100 is "Pumpkin, cooked", not "Pumpkin, canned, cooked" 4 kcal nearer; mushrooms at 80 are "Mushrooms, fresh, cooked with oil", not the plainer "Mushrooms, pickled" 24 kcal further); an estimate's protein, carbohydrate and fat together weigh no more than the food; a reply is asked for at most 20 foods and names any other under unknown_items, inside its 1,000-token cap, and one that lists up to 30 anyway keeps 20 rows and names the rest; a hint that says "unknown" asks no table, and an item with no name at all is no row; Change starts the stepper at ×1 on the row's grams, Undo gives back the scan's count while the grams are still the scan's, and no search on the sheet offers a food the scan put on it; the sheet says "Calories from a photo are an estimate. A photo cannot detect allergens." (RULINGS 2026-09-09, never built on the web before); Kd's eight plates run through the route and the real lookup, with the USDA rows they can reach copied from the table. Merged as PR #74.*
   - 7a-iv. **Replaced before it was built, 2026-09-16, by 7a-iv-a, 7a-iv-b and 7a-iv-c below** (RULINGS 2026-09-16, the portion redesign: every food's own measures, as the professional apps work). What it was, kept for its reasons and its review cases: **A photo's portion by a form, not by words** (Kd, 2026-09-15; RULINGS the same day). Built after 7a-iii-b, from master, its own chat and pull request. Why: four review rounds of PR #71 each found a High in the portion code, because the server reads English — cut words, "pieces", vessel and pack words, the serving names on the food list — to decide what a count counts and what a container holds, and English never runs out of phrases. The rebuild, in Kd's plan: (1) the model's reply carries a short form with fixed choices, written into 7a-iii-b's contract once: its gram estimate, a count of WHOLE pieces only (never slices, chunks or pieces cut from a bigger item; the prompt says so), one vessel from a fixed list (Appendix B's katori, bowl, large bowl, thali section, tumbler and chai cup, with cup, mug, glass, can, bottle, pot, tablespoon and teaspoon, each with its volume), how full, and a size; a value off the list reads as unknown; (2) every food gains its USDA piece weight and cup weight from `food_portion.csv` in the same zips `tools/check-food-sources.ts` reads (SR Legacy writes the measure in `modifier`, FNDDS in `portion_description`; all 313 USDA-cited foods have a household weight and 258 a piece, size or cup weight, measured 2026-09-15), the checker taught to compare them and 7a-iii-a's import carrying them for every USDA food; a packaged product's pack comes from its label as today; (3) the resolver becomes a five-rung ladder over typed inputs and nothing else, for every food a scan prices — until then a USDA food is served at the grams the model saw and our list's foods keep the old rule (RULINGS 2026-09-16) — the person's saved dish × fill · count × piece weight · listed vessel × fill × the cup weight over 240 ml · the model's estimate, marked "estimate" · the serving — with one cross-check: a rung 2 or 3 answer more than three times from the model's estimate gives way to the estimate; the word lists and hint readers in `portion-priors.ts` go, `dishwareGrams` stays the one saved-dish formula and the confirm-time dishware arm reads the same cup weight; (4) the photo sheet names the measure under each item ("2 rotis · 80 g", "Bowl, ¾ full · 260 g", "Estimate · 150 g") and its stepper steps in that unit; nothing else on the sheet moves; (5) tests: a table test over every cell (vessel × count × food kind × saved dish), and a fixed set of test plates — every case from PR #71's four reviews with its expected grams, and Kd's eight plates as the model's real replies — run in CI, so a new finding becomes a new plate and never a new word list; (6) the cost per scan measured on his eight plates before and after (the form adds a few words per item; unverified until then). Also here, from #71's third re-check: L1, a pack word after a brand loses the product ("monster energy can", "yakult bottle" matched before e05de2c) — with the vessel on the form, the hint is the product's name again; L3, "a cup of almonds" and "a cup of rice" reach the 240 ml water cup — closed by the cup weight; item 10's cornflakes defect, the same class; and every container the photo names with a serving word. Today a container is one serving only where its name is exactly "serving", "portion" or "helping", and a plural serving word counts the photo's container only where it is that same word, so the same food reads differently by spelling: six servings of nuggets are 96 g in "serving", "portion", "helping" or nothing, but 16 g in "1 serving", "one serving", "Serving" or "single serving"; three servings of rice are 300 g in "serving" or nothing, but 100 g in "portion", "Portion" or "1 portion" (measured 2026-09-15). A serving is no vessel, so off the fixed list every one of them reads as unknown, and each is its own test plate, all of one food giving the same grams.
   - 7a-iv-a. [x] **Adding a food by measure** (RULINGS 2026-09-16, the portion redesign). From master, its own chat and pull request. Every food the search returns carries its measures and the one it starts at: USDA's own household measures (`usda_food_portions`, named by the importer's rule; a food of our list reads the USDA entry it cites), our list's own serving where no USDA measure of its name weighs within a tenth of it, a packaged product's label serving, and always grams and ounces. Add food, and "Add an ingredient" on a saved meal and on the photo sheet, pick a measure and an amount in one picker — or a saved dish, how full it was its amount — and the server turns the measure into grams from its own list (a measure the food does not have is refused), stores the measure and amount on the meal item (no migration: a meal's `items` are JSON) and keeps them when an edit sends the same grams back. A saved dish weighs by the food's own USDA cup (a "cup" or "half cup" measure) where it has one, else by Appendix B's medium 1 g per ml, so a bowl of cornflakes is no longer weighed as water; and a scan never applies a saved dish on its own (RULINGS 2026-07-18). The photo sheet's scanned rows are 7a-iv-b's. *Built 2026-09-16 on branch `food-measures`: − and + move half a measure (ten grams), an amount can be typed, a dish asks "How full?" before anything is added, and a new dish is saved at Appendix B's global sizes (cup 240 ml, mug 325, bowl 375) or any size typed; a saved meal reads "Apple: 1.5 × medium (3" dia), 273g". From Kd's click-through (RULINGS 2026-09-16): the dropdown is the app's own dark list (`common/Select.jsx`), which opens upward where it would run off the window; a box closes only by its X; "my dish" back to grams keeps the dish's grams with an Undo; the X asks before closing an unsaved scan, and Photo Log brings the unsaved sheet back within its 10 minutes, with "New photo"; the picker and Add food have 44 px controls, 16 px amounts, and rise from the bottom on a phone. Settled in review the same day and the next: USDA's rows of amount 0 whose text states none (frozen kale's 94 g "package") and its "Guideline amount" rows are no measures, nor a serving on import (a database loaded before runs the import again, RUNBOOK); a long measure name is cut where a clause ends and never leaves a bracket open; every food starts at its serving (0 of the 13,225 USDA foods and of our 318 start elsewhere, swept 2026-09-17), an "oz" serving at one ounce where it weighs one; an edit of a meal holding one food twice keeps each item's measure; the close question says the whole minutes truly left, and an unsaved scan is kept from the moment its reply returns, for that person alone, forgotten at sign-out; an amount under 1 g, or one the server refuses, says so. Merged 2026-09-17 on Kd's word (PR #75, CI green, branch deleted).*
   - 7a-iv-b. [x] **The photo sheet by measure** (RULINGS 2026-09-16). After 7a-iv-a, its own chat. A scanned row starts at one of its food's measures only where the scan's count × that measure is within 30 % ~~, or 10 g,~~ of the photo's grams (the "or 10 g" struck 2026-09-17, RULINGS), else at the photo's grams, marked estimate, and is corrected with 7a-iv-a's picker. The word lists and hint readers of `portion-priors.ts` go. Tests: the starting rule as one table over every case before review; Kd's eight plates as the model's real replies and every case of 7a-iv's line above (from PR #71's four reviews) as fixed test plates run in CI, a new finding a new plate. Closes our list's old portion rule (bacon 100 g where the photo showed 30 g; RULINGS 2026-09-16). The model's reply is not changed, so no cost measurement, unless the plan changes the prompt. The scanned rows take 7a-iv-a's roomier look, and a row switched from a dish to another measure keeps its grams with an Undo, as 7a-iv-a's "back to grams" does (Kd's click-through, RULINGS 2026-09-16). *Built 2026-09-17 on branch `photo-sheet-by-measure` (Kd: "go"). Settled while building (RULINGS 2026-09-17): 30 % only, no "or 10 g" — the 10 g started the toast plate's 10 g of almonds, counted 1, at one 1 g almond. Server: `scanStart` in `measures.ts` (grams and ounces are never counted; the nearest near measure, the earlier of two as near; no grams from the model starts where Add food does, an estimate) and `portion-priors.ts` deleted whole; every scanned row carries its food's measures, `startsAt` and `portionEstimated` (`mealPhotoItemSchema`, `pieces` gone), read in one query; an estimate's serving is no measure of its own; every scanned row's rung is `default`. Web: each scanned row is Add food's picker (the "my dish" pill and ×count stepper gone into it), "~30 g · estimate" while a row is at the photo's grams, Change starts the new food by the gram at the row's grams with an Undo, a dish left for another measure keeps its grams with an Undo, or goes back to what the row held where no fill was picked, and the scan's own numbers stand in only while every row is as it started. Tests: the rule as one table (37 cases and an invariant sweep) before review; eleven test plates through the route — Kd's eight and three of PR #71's review cases (counts, cut bits, vessels) — pinned and checked by hand, with the USDA entries our list's foods cite written only where the table lacks them (`fixtures/usda-plate-foods.json`), the same on the loaded table and on a migrated, seeded database with none. Kd's click-through passed 2026-09-17 (*"the tests passed"*); from it (RULINGS 2026-09-17), before the review: the sheet is a short list — one line a food with its amount in words and its calories, a tap opens its picker, one at a time, the photo and total beside the list on a computer and the whole screen on a phone — the amount's unit sits beside it and the picker says "grams" and "ounces", and "Change" reads "Change food". Looked at in headless Edge at 1280 and 390 wide on the real app, the scan's reply served from the toast plate's saved reply (no model call). Kd's look at the short list passed (*"the tests passed"*). Review round one (1 High, 7 Low, 2 weak tests), all fixed: "Change food" keeps a moved row's grams even before the server has priced it (its own measure × amount) and waits for a dish not yet weighed; the total reads "about" while a portion is at the photo's grams; a row the photo gave no grams reads the measure it starts at; one read of the USDA measures per preview, confirm, manual meal and edit; the reply's unread vessel, fill and size are 7a-iv-d. The re-check in the same reviewer chat closed every finding with no Critical/High, and its two Lows were fixed with no further re-check (RULINGS 2026-09-16): "Change food" says truly why it waits (an amount past 10,000 g "can't be weighed", a dish the server refused or gave no answer for, a dish waiting on the other amounts), and a saved meal reads "about" only for a food the scanner priced, since confirming makes every amount the person's (Part 2B §3.2 Stage 4). Merged 2026-09-17 (PR #76, CI green, branch deleted).*
   - 7a-iv-b-ii. [x] **How far over a target** (Kd, RULINGS 2026-09-17). After 7a-iv-b, its own small chat. Today a day past its protein, carbs, fat or calories shows "Remaining today" 0 and a full ring. "Remaining today" says how far over instead ("30 g over", "120 kcal over"); the ring stays full and its middle already counts past the target. Nothing is blocked, no warning or judging words, and a past day's "Eaten on this day" is unchanged. Web only; a render test for under, at and over each of the four. *Built 2026-09-17 on branch `how-far-over`: the card's numbers are signed, a row below zero reads "N unit over", and every row puts a space before its unit ("1200 kcal"), the two older tests that pinned "1560kcal" and "90g" updated. Tests: under, at, over and mixed across all four, a fraction that rounds to 0 never "0 g over", "about … over", and a past day with no "over"; 4 deliberate breaks each red, the page restored identical. Kd's click-through passed 2026-09-17 (*"allpassed"*). The review found no Critical/High; its Lows fixed with no re-check (RULINGS 2026-09-16): the ring reads "/ 140 g" as the card does, a -0 case the API cannot send is gone, and the past-day test is named for what it pins.*
   - 7a-iv-c. ~~The last measure and amount remembered per food.~~ **Struck 2026-09-17, never built** (Kd, RULINGS 2026-09-16's portion line as amended): the amount is not a habit, and the measure alone saves one dropdown tap for a new table of what each person eats. Every food starts at its serving, as 7a-iv-a built.
   - 7a-iv-d. [x] **The scanner's reply without the fields nothing reads** (found by the review of PR #76, L7). Since 7a-iv-b the model is still asked for each food's vessel, how full it looked and its size, and nothing on the server reads them: a row starts by the count and the grams. RULINGS 2026-08-24 as amended trims the reply to what the app reads, and a trim changes the prompt, so the plan measures the eight plates' cost before and after (at most ten calls, RULINGS 2026-09-15) and Kd rules: trim them, or keep them for later portion work (the person's dishes, reference objects). Its own chat. *Built 2026-09-17 on branch `scanner-reply-trim`; Kd: trim them (RULINGS 2026-09-17). One list per food is now `[name, canonical_hint, count, grams, kcal, protein_g, carbs_g, fat_g]`; the prompt loses the three slots and the vessel list, every plate-reading instruction word for word. The first shape check came back as its object in a list, `[{…}]`, every food well formed and refused as unreadable, so the prompt asks for "one JSON object only" and the contract reads a list of one as what it holds (two objects, an empty list or a list in a list are still no reply); the second came back as one object. The eight plates: $0.000625 a scan, 563 tokens in and 36.5 out a food on average, against $0.000803, 612 and 45.0 on 2026-09-16. The toast plate came back as 3 lumped dishes, so, on Kd's word, six more scans of it alternated today's prompt and the trimmed one: 3, 5 and 3 foods at 640 tokens in, against 9, 5 and 5 at 568 — the lumping is not the trim's (7a-iv-f). Sixteen paid calls, $0.0107. The eleven test plates keep their replies with the three slots taken out and every pinned row unchanged; the unit tests read two real replies to the new prompt (the salmon plate, and the one in a list). No screen change; Kd's smoke of two real scans on the local app passed (*"smoke passed"*). The review found no Critical/High; its three Lows fixed with no re-check (the spec's note, a per-food figure, a handoff slip). Merged 2026-09-17 (PR #79).*
   - 7a-iv-f. [x] **The scanner lists each food, not a dish of them** (found building 7a-iv-d; Kd chose to make it its own card, RULINGS 2026-09-17). On the toast plate the model names "avocado toast with egg and bacon" and "open sandwich with ham, cheese and apple" in one reply and all nine foods in the next, with the same prompt, so the lumped rows are estimates, the almonds are lost and our list's numbers go unused (RULINGS 2026-09-15: every food it sees, each priced from a table where one has it). The plan proposes the words that ask for each separate food and measures them on the toast plate and the eight before Kd rules: they change the plate-reading instructions, which are his (RULINGS 2026-08-24). **Built NEXT** (Kd, RULINGS 2026-09-17), before 7a-iv-e; the cost he was told, to be measured: about $0.00068 a scan over the eight plates, against $0.000625 today. *Built 2026-09-17 on branch `scanner-lists-each-food` (PR #80; Kd: "ok build"). One sentence joins the prompt after what an item is, every other word unchanged: every food the model can see apart is its own item — "toast topped with avocado, a fried egg and bacon is four items" — and a dish cooked or mixed into one, or a closed sandwich or burger, stays one (RULINGS 2026-09-17, where the line sits). Twelve paid calls, $0.0102: the eight plates listed every food apart, 56 foods against 40 in 7a-iv-d's run, none lost, $0.000791 a scan (665 tokens in, 236 out); the toast plate and the banana-and-egg toasts, scanned twice more each, came apart all three times — the toast plate is the prompt's own example, the banana-and-egg toasts are not. Priced on the loaded table, 43 of the 56 come from a table against 29 of 40 before, but the model's names turned descriptive: five foods a table priced before are now estimates (asparagus and sausage on download (2), pumpkin and broccoli on (3), broccoli on the minimalist plate), and three rows name the wrong food — "sweet corn" as USDA's "Corn, sweet, white, raw" on (3) and the minimalist plate, and "bread" as our list's "Whole wheat bread" on (6). The review of PR #80 found those rows (High 1): 7a-iv-i, and whether #80 waits for it is Kd's. Tests: the sentence pinned, and where it stands; the eight plates' new replies are `-apart` plates beside the old ones, every row checked by hand and the wrong rows pinned as they read, their USDA rows and cited entries copied, the same on the loaded table and on a migrated, seeded database with none; the test plates take one app per eight plates, since eleven on one app passed the sign-in limit and shifted every later plate's reply.*
   - 7a-iv-i. [x] **A scanned name finds the food it means** (found building 7a-iv-f; High 1 of PR #80's review). Since 7a-iv-f the model's names are more descriptive, and the lookup reads them wrong two ways: a name no table holds whole ("steamed broccoli", "mashed avocado", "grilled asparagus", "scored pork sausage", "roasted pumpkin", "iced latte") is the model's estimate though a table has the food — 11 of 13 estimates on 7a-iv-f's eight-plate run; and a name USDA holds only as another form of the food wins over our list's plain one ("sweet corn" is "Corn, sweet, white, raw" for boiled corn, where our list has "Corn (cooked)"), while a bare "bread" is our list's whole wheat. Measured with the real lookups (2026-09-17): trying our list by a name's shorter ending before USDA repairs corn, broccoli, asparagus, avocado and sausage, but makes "garlic bread" Whole wheat bread (USDA has garlic bread) and "chicken salad" an estimate; after USDA it leaves corn wrong. So the plan settles the order of our list's whole name, USDA's whole name and head, our list's shorter ending, USDA's every-word match, and what a name several of our foods answer to means ("bread"), as one table test over every class — the eight plates' old and new hints, the cases above, compound foods (chocolate milk, almond milk, coconut milk, peanut butter cups, fried rice) — before review. No word list, no model call. **Built NEXT** (Kd, RULINGS 2026-09-17), its own chat, from branch `scanner-lists-each-food`, its own pull request to master: PR #80 waits for it, then #80's reviewer chat re-checks High 1 against it, #80 merges and this card after it. *Built 2026-09-17 on branch `scanned-name-finds-food` (Kd: "go"; RULINGS 2026-09-17, two lines). The order: our list by the whole name · USDA's food of that name (description or head) · our list's food of a shorter name, the first words dropped, the longest it holds, never past a dropped word that is a food of its own (our list finds it alone, or a USDA description is it or has it as head: `usdaFoodWords`, one query), never our pick for a word several foods answer to (`COMMON_FOOD_PICKS`, split from the aliases; `findCuratedNamed`), and only where the food at the model's grams carries its kcal within 30 % or 10 kcal, so never without the model's figures · USDA's food of every word, taken over that only where more than 10 kcal per 100 g nearer the model's energy · a packaged product · the estimate. A whole name's answer that fails the 3× check still asks no other table that name, but a shorter one. Names of more than ten words are not shortened. "Bread" alone is white bread. Measured before building on the loaded table (scratchpad, real lookups): the reviewer's shorter-ending order made "light coconut milk" whole milk, "scotch egg" an egg, "ham sandwich" a turkey sandwich and "pork buns" a bread roll among 229 typed names, and USDA by a shorter name worse ("roasted vegetables" pickled, "chia pudding" bread pudding); the rule built changes 39 of 382 names, and the eight "-apart" plates' estimates go from 13 of 56 foods to 5 (sourdough toast twice, toast, cherry tomato, roasted pumpkin), both sweet corn rows are our cooked corn, and none is a wrong food. Tests: the order as one table (18 cases, and the energy bounds on both sides, 30 % and 10 kcal) with fake lookups; picks never answer a shortened name, white bread; `usdaFoodWords` on fixture rows (head, name, a word a description only starts with, unpriceable, wildcards and accents not asked); two new test plates through the route, `review-shorter-names` and `review-names-kept-whole` (25 foods: every class above, compound foods, peanut butter cups still peanut butter as ROADMAP 10 has it, and lemon water, the only row that proves a USDA-only food word blocks a shorter name), their 60 USDA rows copied in USDA's id order (a copy in another order named Greek vanilla yogurt on an empty table) and 13 cited entries, the same on the loaded table and on `aihg_ci_scratch` with none; ten deliberate breaks each red, the files restored identical. Found, and raised with Kd: plate (6)'s "bread", now white bread, starts its two slices at the nearest measure, "2 × cup, cubes" (70 g), USDA's 58 g of two slices being further — the start rule's (7a-iv-b), pinned as it reads.* *Fixed the same evening, from PR #80's re-check and PR #81's review (Kd: "solve men"; RULINGS 2026-09-17 amended): calories cannot tell a food from its stand-in, so "vegan butter", "soy yogurt", "lactose free milk", "vegetable lasagna" and "veg biryani" read as our butter, yogurt, milk, meat lasagna and chicken biryani, and two slices of toast as "2 × cup, cubes". Now a name is shortened only past words of how a food was cooked, cut or served (`PREPARATION_WORDS`: LanguaL's cooking methods, forms and heat treatment, sizes, how warm it is served, the noise words), to the first of our food's versions whose own name those words do not contradict (`findCuratedVersions`; `contradicts`: cooked, raw, kept, and LanguaL's four ways of cooking), so `usdaFoodWords` and `findCuratedNamed` are gone; sweet corn, jacket potato, basmati and jasmine rice and caffè latte are aliases; and a scanned row of our list's food starts at its own serving where the photo's count of it is within 30 %, before a nearer measure. Measured on the loaded table (618 names: the plates' 168 rows, the last chat's 234 typed names, both reviews' 28 and 188 probes of each class): 71 answer differently from 7a-iv-i, each reviewer's row is now USDA's food or the estimate, "steamed carrots" are our cooked carrots, not raw; masala dosa and steamed dumplings are estimates, deli ham and green tea USDA's, and the banana-toast plate's banana, seen at 100 g, starts at "1 × banana · 120 g" where USDA's small was 101 g. Three test plates (`review-substitutes`, `review-dish-versions`, `review-cooking`), 37 more USDA rows copied in id order and 4 cited entries, the same on the loaded table and on `aihg_ci_scratch`.* *Both re-checks then found one thing each, fixed the same night: the fix had let a shortened name land on the list's pick for a word several foods answer to, which a0fc206 refused — "roast pork" read as our pork chop over USDA's roast pork, "stewed beef" as mince, "whole chicken" as breast — so `findCuratedVersions` reads by the names of one food alone (`NAMES_BY_STEM`), as `findCuratedNamed` did; and "iced", which one market reads as frosted (an iced bun read as a bread roll), left `PREPARATION_WORDS`, with `iced_latte` an alias of our latte so the plates keep theirs. A fourth plate, `review-picks` (stewed beef, whole chicken, fried cheese, iced bun, grilled sausage), 18 more USDA rows and pork chop's cited entry; three more breaks each red. The re-check of that found the same pick made by the list's own order — our one "Sandwich (turkey)", "Burrito (chicken)" and "Lasagna (meat)" answered every shortened sandwich, burrito and lasagna name, so a grilled cheese read as turkey — closed by `saysAnotherKind`: a shortened name never lands on a food of the diet ladder's egg or meat rungs whose brackets name a kind the scanned name did not say, while a food no rung above vegetarian is left alone ("grilled pizza" is still our cheese pizza, "boiled rice" our white rice). Of 660 census names it changes three: "grilled sandwich" to USDA's grilled cheese, "baked lasagna" to USDA's vegetable one, "grilled steak" to an estimate, our steak's name saying sirloin. Three rows more on `review-picks`, 7 USDA rows; three more breaks each red.*
   - 7a-iv-g. [x] **Change a food already logged** (Kd, RULINGS 2026-09-17). Today a saved meal can be renamed, re-timed, relabelled, given an ingredient or deleted, but a food in it cannot be changed. Tapping a logged food opens the measure picker at its measure and amount, and the server re-prices it from the food list (the meal-edit route already re-measures an item); and "Type my own numbers" takes protein, carbs and fat for that food, works out its calories, stores the row marked as the person's numbers (not an estimate, not a table's) and shows it so. The plan settles: what a typed row does when its amount is changed after, removing one food from a meal, and alcohol (calories no macro explains). Its own chat; its place in the order is Kd's. *Built 2026-09-18 on branch `change-logged-food`, before 7a-iv-h (Kd: "go"; RULINGS 2026-09-17 as settled 2026-09-18). A saved meal lists its foods one a line ("600 g · 990 kcal", "· estimate", "· your numbers"); a tap opens the food's box at the measure and amount it was logged by (a saved dish, or a measure the food no longer has, by its grams), with the server's numbers for a new amount before it is saved, "Type my own numbers" (protein, carbs, fat for the amount shown; "Comes to N kcal" from the server; the alcohol line), "Use the food list's numbers" or "the scan's estimate" for a row of the person's own, and "Remove from this meal", which on a meal's only food asks to delete the meal. Server: the meal PATCH's items may name the saved item they are (`from`), keep one exactly as saved (`{from}`), and carry `own` (set, or null to take away); a saved item priced by the person's numbers is `own`, carries them per 100 g and keeps the scan's estimate it was typed over (`scanEstimate`); numbers heavier than the food are refused (`own_numbers_too_heavy`), an edit naming an item that is not there or is another food is 409 `meal_changed`. Two routes: `POST /v1/nutrition/meals/:id/preview` (the edit priced by the same rule, nothing saved) and `GET /v1/nutrition/meals/:id/measures`. Adding an ingredient now keeps the meal's other foods exactly as saved instead of pricing them again (a packaged product past its day in the cache no longer blocks it). No migration, no new package, no model call. Kd's click-through passed 2026-09-18 (*"excellent all passed"*); from it (RULINGS 2026-09-18), a meal's food lines fold away behind a dropdown button beside its Delete, hidden until tapped. From the review (2 High, 2 Low): every meal carries `itemsVersion`, which an items edit sends back and the server checks before pricing and again under the row's lock (409 `meal_changed`), so a food added in another tab is never dropped and two saves never overwrite each other, and a PATCH writes only the fields it sends; nothing is saved or typed over until the server has priced the amount on screen; an expired packaged product is never searched for by its slug; a failed delete keeps the box open. The re-check ended the round with nothing Critical or High open; its two Lows fixed with no further re-check (a meal just saved goes on the page at once, so the next food is changed from it; add ingredient's refusal wording). Merged 2026-09-18 (PR #83).*
   - 7a-iv-h. [x] **Amounts anyone can read** (Kd, RULINGS 2026-09-17). Web only, no model call. ~~(1) A row at the photo's grams also says them in a household measure the food already has — "~260 g · about 1 cup" — where it has a cup measure; the plan settles the rounding (quarters of a cup?), which measures count as household words, and what a food with none shows.~~ **(1) struck 2026-09-18, built and taken off at Kd's click-through** (RULINGS 2026-09-17, as amended). (2) The "How full?" buttons carry a small bowl drawn filled to that level, in the library's line style, and a word — Quarter · Half · Three quarters · Full — and the amount lines read "My bowl, half full · 180 g", never "½ full". After 7a-iv-f; its place beside 7a-iv-e and 7a-iv-g is Kd's. **After 7a-iv-g** (Kd's "go" to 7a-iv-g's plan, 2026-09-18), then the member list. *Built 2026-09-18 on branch `amounts-anyone-can-read` (PR #84); (1) was built, seen by Kd and taken off the same day, so a photo row reads "~60 g · estimate" as before; the bowls are drawn filled by VOLUME, as the server weighs a dish, so Half sits at two thirds of the height and Three quarters near the rim; a saved cup or mug is drawn as a mug, filled by height (the review of PR #84). No migration, no route, no package. Merged 2026-09-18 on Kd's word after the review (*"fix … and merge"*); his click-through spoke only of the cup words.*
   - 7a-iv-e. [x] **Your own ring numbers** (Kd, RULINGS 2026-09-17). After 7a-iv-d, before the member list, its own chat. A switch over the Nutrition rings: App's plan · My own (Gym's plan joins at Stage 2 item 10; an option with nothing behind it is not shown). The picked numbers feed the rings, "Remaining today" and its "over" rows. "My own" edits calories, protein, carbs and fat, starts from the app's numbers, is stored on the server per person (tenancy and the stranger test, the privacy export, account deletion) and can be switched back. The health rules hold: never below the app's calorie floor, and a plan with no calorie cut (a health yes, Safe mode, under 18) takes no typed calories below keeping the weight — refused by the server, said on screen. How the four numbers agree with each other (macros that add up to the calories) is the plan's to settle. 7c's day targets then read the picked numbers. *Built 2026-09-18 on branch `own-ring-numbers`. A switch over the rings — App’s plan · My own — with each side’s own calories under it; "My own" opens an editor filled from the app’s numbers (or the person’s own, once typed), and the four numbers it saves feed the rings, "Remaining today" and its "over" rows. Switching back keeps them, and one tap sends them again. Migration `0032` adds `user_nutrition_targets`, one row per person, written only when the switch is touched; its CHECKs are rails (the source, all four numbers or none, ’own’ never without them, 0–20,000 kcal and 0–2,000 g) and NOT the health rule, which is computed per read. `PUT /v1/nutrition/targets` takes `{source:"app"}` or `{source:"own", kcal, proteinG, carbsG, fatG}` and answers with the whole targets payload; the contract carries both sets, which one is showing and why a stored set cannot be used today, picked or left behind "App’s plan" (`appTargets`, `own`, `source`, `ownHeld`), with refines that refuse every impossible pairing. The rule (`ownTargetsHeld`): never below the app’s 1,200 kcal floor, and never below what keeps the weight for a plan that holds no cut — read from the plan’s own no-cut REASONS, not its `no_deficit` flag, which is only raised on a "lose" plan (a 17-year-old who picked "keep my weight" is refused too). It runs on the way in AND on every read, so a number allowed in March stops feeding the rings when the health question is answered yes in June — the numbers are kept, the app’s plan shows, and the screen names the figure that holds them. A stale target blanks the app’s numbers only, so the "new target" card keeps the switch for someone with numbers of their own. The macros are typed freely, with what they come to said beside the calories. The table is on both privacy lists (Day-14 delete, export). Tests: the rule as one table (20 classes, both bounds and every reason) before review; the contract’s impossible pairings; the route end to end (the refusals, the switch back keeping the numbers, the health answer changing under a stored set, the under-18 hold, tenancy over two people); migration `0032`’s rails by causing each; and the screen’s wiring end to end. 16 deliberate breaks, each red on the test that claims it, every file restored identical.*
   - 7a-v. [ ] **More than the macros** (Kd, 2026-09-16; RULINGS the same day). After the member list (RULINGS 2026-09-17, order), its own chat. Fibre, total sugars, saturated fat, sodium, potassium, calcium, iron, magnesium and vitamin D, from the USDA columns 7a-iii-a imported and never from the model: a USDA food reads its own row; one of our 318 reads the USDA entry it cites (313 of them), and a CoFID food carries fibre and "not known" for the rest until the checker reads CoFID's other sheets; an estimate row reads "not known"; a packaged product reads what its label gives. Shown on a saved meal, on the photo sheet's rows, and as a list under the Nutrition page's rings (no new rings), each against a published daily reference where one exists — the plan names the source and its figures by age and sex before building, and a reference is never a target the app sets. The day's line says "about, N items not known" when a row lacks a figure. Stored per item in the meal's `items` (no new columns), so a day is the sum of its meals. No model call; no new dependency.
   - 7a-i-b. [ ] **More foods: healthy staples and each market's traditional dishes** (Kd, 2026-09-14; RULINGS the same day). Healthy foods common everywhere, fruits among them (Kd, 2026-09-14), and the traditional foods of the US, Canada, Europe, Australia and New Zealand first, then Brazil and Asia. The same rules as 7a-i: every number from an official food table and never a website (USDA and CoFID first; for a dish they lack, the market's own table — the plan names each and checks its licence for a paid app), each food's diet by what it is made of, `tools/check-food-sources.ts` taught each new table. A food people count by the piece but the list serves by weight (chicken wings and drumsticks, lamb chops, shrimp, scallops) gets USDA's per-piece portion, so a photo's count can multiply it (the re-check of PR #71, its Low 5); the manual Add food screen starts at the same serving, so the plan keeps a plate of shrimp to a few taps. Comes before 7a.
   - 7a. [ ] **Meal suggestions.** From the food table by diet, meals a day and calories left (no cuisine filter and no cuisine data, RULINGS 2026-09-12); a food-allergy caution line on every suggestion (RULINGS 2026-09-09 — no allergen tags, no allergy question; its words are `FOOD_ALLERGY_CAUTION` in `@app/shared`, already under the meal ideas after a workout); under-18 and Safe mode rules respected. Someone with no diet or meals answer (everyone who finished setup before 4b-ii) is asked for it, never given one. The post-workout card's ideas (`summaryContent.ts`) come from this same engine once it exists — what to eat now, given what the workout burned and what is left today — so there is ONE source of food ideas; decided in this card's plan. The ideas VARY: drawn from the food list afresh each day, and per workout on the post-workout card, never the same lines every time — Kd asked on 2026-09-13 whether the post-workout ideas repeat each day, and today they do (seven fixed lines in three bands) until this card replaces them. A non-vegetarian's suggestions include vegetarian and vegan dishes; the diet is a ceiling, never a demand (RULINGS 2026-09-13). The foods' diet labels (7a-i) follow a food's usual recipe and cannot see a product's own; **one line to Kd in this card's plan:** whether the caution under a suggestion also tells a vegetarian or vegan to check labels (bread can hold milk, some cheese animal rennet).
   - 7a-ii. [ ] **The dashboard's Recommended box, with options** (Kd, from his click-through of 4b-ii's fixes; RULINGS 2026-09-13). Today the dashboard's "Recommended For You" box reads the OLD backend's `/recommendations` (`recommendationApi.js` → `mlApi`), which is down, so it prints "Recommendations are unavailable right now." — item 8's class of dead screen. It becomes ONE box with a row of options across its top — **Exercise · Food · Running** — and the tapped option fills the box with that kind of recommendation; nothing else on the dashboard moves. Built here with its first option, **Food**, reading 7a's suggestions (the same engine, never a second one). **Exercise** joins at 6b (today's session from the week's plan) and **Running** at 7b. An option whose plan is not built yet is not shown, so the box never promises a kind the app cannot make; and the box says, in plain words, what each kind is and why it is empty (Safe mode, no run answer). The post-workout card stays as it is. **Two lines to Kd in this card's plan, before building:** (1) the design as read above — is that it? (2) Running on the WEB: RULINGS 2026-08-24 sent running to the phone app ("available in the app") and 7b's line says the same, but his ask names Running as an option on the web dashboard; recommended: the web shows the week's runs too — reading a plan needs no GPS, only recording a run is phone-first.
   - 7b. [ ] **The run plan.** A week's run schedule from the running answers, which are asked here when someone opens running, not in setup (RULINGS 2026-09-13; the questions are on the 4b-iii line) (phone app; web says "in the app" — unless Kd rules at 7a-ii that the web shows the week's runs); adds the **Running** option to the dashboard's Recommended box (7a-ii); none in Safe mode. The rules the running community accepts (RULINGS 2026-09-10): a person who cannot yet jog 30 minutes gets a walk-run progression (the Couch-to-5K pattern the NHS publishes: three days a week, run intervals grow and walk breaks shrink over about nine weeks, starting from the jog band they gave); a runner starts at the weekly distance they run NOW, never more, and it grows gradually (about 10 % a week at most) with an easier week every third or fourth; one long run a week; most running easy and at most one hard session a week for a beginner, two for an experienced runner; a rest or easy day after a hard one; training paces only from a REAL recent time (Jack Daniels' published VDOT formula), never guessed — no recent time means paces by feel ("easy: you can talk"); no run plan for a "no" to "Do you run, or want to start?"; none in Safe mode; the plan builder's circuits (6a) start from the fitness, push-up and plank answers, since setup no longer asks the jog band. **To settle in this card's plan** (Kd, 2026-09-13, said in passing, not yet a ruling): *"i think running recommendation is not needed but we will have a option in the running feature i mena the page where there will be some advice to go from a beginner to a pro but it will be build in the mobile apps not the browser"* — a plan made from the answers, a beginner-to-pro advice page, or the page shaped by the answers; and how that fits the Recommended box's Running option (RULINGS 2026-09-13, 7a-ii).
   - 7c. [ ] **Your own diet plan** (Kd, 2026-09-12): the app's suggestion is a starting point, never the only choice. Swap a meal, add your own, change a portion, and keep it as your plan for the week — the way the weekly workout plan (6b) and the run plan (7b) are edited. Nothing like it exists: no meal-plan table, no routes, no screen, so this splits into the plan and its rules on the server and then the screen when it is built. The day's calorie and macro targets are the plan's rails, and Safe mode and the under-18 rule hold here as they do everywhere (RULINGS 2026-09-07).
   - 7d. [ ] **Your week in food** (Kd, 2026-09-16; RULINGS the same day). After 7c, before launch, its own chat. On the Nutrition page: the last 7 and 30 days as a chart of calories and of protein, carbs and fat per day against that day's targets (the plan's, 4a-iii), a day marked "about" where an estimate was logged, and plain facts under it — "Protein was under target on 5 of 7 days" — never advice beyond the plan's own numbers; fibre and sodium join once 7a-v stores them. Read from the meals already saved; no model call; the chart drawn with what the app already uses, or the plan asks Kd for a dependency. Safe mode and under-18 change nothing here (the targets already carry their rules).
8a. [ ] **What a gym can see, in the person's own terms** (Kd's click-through of 4b-ii, RULINGS 2026-09-13). `components/gym/OrgVisibilitySheet.jsx`, drawn on the join door, onboarding's screen 11 and Settings → Gym. The AI-coach line and the "before you joined, or after you leave" line go. Meals and nutrition, weight and body measurements, and run routes stop being flat "Never" and become "only if you choose to share". **Raise with Kd in this card's plan, with a fix and a recommendation:** there is no sharing control anywhere in the app today, so "if you choose to share" describes a choice a person cannot make — the same class of false-on-screen line the old "Helps the AI give safer advice" hint was. Recommended: word it so it is true today and stays true when the control lands ("never — unless you choose to share it"), and build the control itself as its own line rather than inside this one. Kd's exact words are in RULINGS. Also on this card: Kd quoted the join panel's "If you're standing at the front desk, ask them now — it takes one tap" and did not say what should change about it — ONE line to him before building, never a guess.
8. [ ] **Dead web screens off the old backend**, one screen per chat. Predictions, workout templates, profile photo (needs image storage), badges/challenges/leaderboard "coming soon" — each either moved to the new API or shown as "in the app", never left broken. (The dashboard's dead "Recommended For You" box is 7a-ii, not here.) Delete account moved to the new API on 2026-09-07, with the undo page (`/restore-account`) built the same day after review.
9. [ ] **Chat coach switched off** on the web (route and import), cost counter moved out of the coach service. Kd's ruling; still live and billing today. **Ask Kd in this card's plan:** on 2026-09-13 he said *"the chat part will be completely deleted"*, while RULINGS 2026-08-18 keeps the coach off, not deleted from the code.
10. [ ] **Known defects on the way**, one defect per chat, each starting with a failing test: a network blip on page load logs you out · a dropped request logs you out instead of offering retry · sync upload has no timeout · the full API suite flakes because nine test files share one seeded database (one more seen 2026-09-16: `auth.routes.test.ts`'s setup failed at its delete of `p21-%` users, a pattern that also takes `workouts.sync.test.ts`'s `p21-sync-%` users while that suite runs; it passed alone and in the next full run, and its error was not kept) · a workout's calories use the weight on the day it syncs, not the day it was done (spec Part 4 §3.6 asks for the newest weigh-in at or before the workout; the two differ only for a workout synced after a later weigh-in) · the web test `xpDisplay.render.test.jsx` "a non-array recommendations field does not blank the page" reads the recommendations message the moment "Level 3" shows, without waiting for it, so a slow CI runner fails it (seen once on PR #60) · the web test `settingsHealth.render.test.jsx` "says so when the health answer cannot be read, and Try again reads it again" waits the default one second for "Try again" and fails in the full local run while passing alone (seen 2026-09-13, on the committed 4b-ii code too) · a food on the list measured by the cup is weighed in a known container as if it were water, so cornflakes (30 g a cup) in a cereal bowl are 375 g, and a photo's count of two bowls 750 g (found 2026-09-15; 7a-iv-a weighs a saved dish by the food's own USDA cup, and 7a-iv-b portions a photo by the food's own measures — RULINGS 2026-09-16) · the food list's matcher drops a noise word that is a food's own name, so a photo's "peanut butter cups" (the sweet) are logged as peanut butter and "tea leaves" as a cup of tea (the rule a product's name now follows, a noise word needed unless it measures the food before "of", would lose "banana slices" and "pizza slice", so the list needs a rule of its own, planned in its card; found 2026-09-15) · USDA's food of every word may hold words the scanned name never said, so "spinach lasagna" is USDA's lasagna with meat and spinach, "vegetable stir fry" its stir-fried beef and vegetables and "smoked ham" its honey smoked ham (as before 7a-iv-i; found 2026-09-17 in the census for PR #81's review) · a food our list serves by weight has no serving for a photo's count to start at, so a pan-fried chicken breast seen at 172 g starts at USDA's nearest measure, "1 × cup, chopped or diced · 140 g" (pinned on the minimalist plate since 7a-iv-b; found 2026-09-17).

## Stage 2 — The gym console, remaining

Order (Kd, 2026-09-13, reshaped 2026-09-17; RULINGS both days; Stage 1's food leftovers 7a-iv-b-ii, 7a-iv-c and 7a-iv-d come first, RULINGS 2026-09-17): **the app sits BESIDE the gym's own software** — the gym's software is the source of truth for who is a member; ours copies from its uploads and reconciles, and never gates. **Items 3 and 5 first**, as ONE design ("the member list") in two halves; then 1a, the "active day" definition, before either screen that reads it. The numbers below are kept so nothing that names them goes stale.

1. **The leaderboard**, on active days.
   - 1a. [ ] **"An active day"**, the shared definition as code (server and `packages/shared`; RULINGS 2026-09-17): a visit from any source, or a workout in the app — a workout counts on its own, visits count when a source exists, and the gym sees which. Read by the leaderboard, "slipping away" and the cheer; the streak already counts both. The visit tiles show visits only where a source exists and say so otherwise. No screen; ticked on merge.
   - 1b. [ ] Gym leaderboard (R1–R18 of 2026-09-07): one ranked list, filters, member's My Gyms view, goal points, greyed opt-out, info symbol saying what counted.
2. [ ] "Slipping away" web half — its quiet window counted in active days (1a), not days marked present — + message expiry after 7 days (server half is built, unreviewed; re-pointed at 1a).
3. **The member list, server half** — split 2026-09-17 into three parts, designed together and built back to back after Stage 1's food leftovers (7a-iv-b-ii, 7a-iv-c, 7a-iv-d; RULINGS 2026-09-17), with item 5 right after them. Kd approved the plan the same day.
   - **Planned in full on 2026-09-19** (a Fable chat, no code; RULINGS the same day): the whole design is spec Part 3 §9 (`docs/spec/03-part3-org-console.md`), which every chat below reads first. **Kd's flow, the same day (RULINGS 2026-09-19), is in every line below:** the list keeps each person's STATUS in the gym's own word and can be filtered by it; nobody is emailed until staff press **Invite**, for everyone or for the statuses they tick; the same upload serves a gym with other software, a gym leaving other software, and a gym with none, which then keeps its list in the app by hand; staff add a walk-in and invite him. Fees, renewal dates and payments are NOT in it (§9.11). 3a was one line and is four (the rule at the top of this file: more than a thousand new lines, and it touched both uploads and other people's data). Every part: **Opus xhigh**.
   - 3a-i. [x] **Opening a member file safely** (§9.4). Server only, no database, no route. The file is told by its first bytes, never its name. An Excel file is unzipped by our own bounded reader and rebuilt before `read-excel-file` sees it — measured 2026-09-19: a 139-byte file made that package take 200 MB, so a 5 MB one could take the API down. CSV is decoded by the ladder measured on Excel here (UTF-8 with its mark, UTF-16, Mac, Windows-1252) and parsed by one table-tested reader (`,` `;` TAB `|`, `sep=`, quoted line breaks). All of it runs in a worker thread with a 15-second limit. Out comes a grid of text cells, or a refusal whose words say the fix. Tests: real files made by the Excel on Kd's machine, one Google Sheets pair Kd downloads once, booby-trapped files built inside the test. New package: `read-excel-file` (Kd's yes, 2026-09-17). Ticked on merge. *Built 2026-09-19 on branch `member-file-open`. Kd, asked, kept the formats to CSV and .xlsx and added one refusal (RULINGS 2026-09-19): a file named like Excel that holds a web page or Excel 2003 XML. Found and fixed while building: Node 24's own Windows-1252 decoder turns 27 letters and signs into control characters (Node 22's does not), so 1252 is our own table; CI found that a worker told `--import tsx` runs without tsx on Node 22, which production runs, so the worker starts on a two-line JavaScript entry that loads it with tsx; Kd's Google Sheets download showed Google stores numbers in exponent form (`9.1987654321E11`), so a number cell is written out in plain digits by moving the point in the text; Excel's Mac CSV ends its last line in CRLF, so Mac line ends need only be "at least as many"; a workbook declaring more than 25 MiB of XML is `too_complex` (the fix is the member sheet as CSV), not `unsafe_archive`. Review round one (fresh chat, which RAN it on Node 22) found a Critical — a 0.8 MB workbook pointing 300,000 cells at one shared string took the request thread past 1 GB — now a budget of cells and characters for the whole grid, counted in the worker; two Highs — a 2 KB part with one long tag held a reading slot for the whole 15 s (the Excel package takes 74 s over a 1 MiB tag), now a tag limit of 64 KiB outside quotes and a linear hidden-row pattern; and a refusal that spoke of the file's name, which the server never sees — every sentence now true of every file that draws it. Measured on Excel's own 10,000 × 30 files on Node 22 after the fixes: .xlsx 1.4–1.6 s and +102 MB peak, CSV 0.6–0.9 s and +37 MB, the request thread never stalled over 24 ms (spec §9.9).*
   - 3a-ii. [ ] **Understanding the file** (§9.5). Server only, no database, no route. Finds the header row and the name, email, phone, member-number and STATUS columns — header words from real products' files, confirmed by what the cells hold; an "emergency contact" column is never the member's and a "payment status" is never the membership's; the status is kept in the gym's own word ("Active", "Frozen"), never turned into ours — and cleans every row: the email by sign-in's own rule, the phone to one form for the gym's country (`libphonenumber-js/max`; the 52 vectors measured 2026-09-19 are the table test), numbers Excel shortened (`9.19877E+11`) said so and never repaired, an address on more than 5 rows treated as a front-desk placeholder, a row with no email and no phone skipped and counted. `tools/read-member-file.ts` prints what the server understood of a file on disk. New package: `libphonenumber-js` (Kd's yes, 2026-09-17). Ticked on merge.
   - 3a-iii. [ ] **The list on the server** (§9.6–9.9). One migration (three list tables; `last_listed_at` and `stated_phone_e164` on the membership). An upload is either "my whole list" or "add these people" (adds and changes, takes nobody off); a status that changed updates the person in place; the list reads back filtered by status, with a count for each status word; a confirm emails NOBODY. Upload → preview (nothing changes) → confirm; a confirm applies to the list the preview was worked out against, or refuses; one file is read at a time per gym, beside the two a process (review of PR #85: two slow files held both slots for every gym); the same file twice writes nothing; a change of more than `max(10, 10 %)` needs a tick on that one request; the names behind every number can be read before confirming; one pure `reconcile` rule with one table test; staged uploads expire after an hour; the list goes when a gym closes. No screen; ticked on merge; the reviewer runs it with several gyms and staff.
   - 3a-iv. [ ] **Keeping the list by hand** (§9.9) — what a gym with no other software lives on. Add one person (name, email or phone, member number, status); change one; take one off; put an app member who joined by code on the list in one tap; remove everyone "no longer on your list" (or "never on your list") in ONE call that names the list version and the count the person saw, refuses if either has moved, carries the same large-change guard, and tells each person as a removal does today. Staff and the owner are never in those groups. No screen; ticked on merge.
   - 3a. *(The line as first written, 2026-09-17, kept for its words; built as 3a-i to 3a-iv.)* **The list itself, server only.** A gym sends its whole list as CSV or Excel (`read-excel-file`); the server guesses the name, email, phone and member-number columns and answers with the guesses and the counts before anything changes; on confirm it compares the list with the app — new people (marked "to invite", no email yet) · app members no longer on the list (flagged; one call removes them all, each told as a removal is today) · app members not on the list; the same file twice changes nothing; one person can be typed in. Phones stored in one form for every gym (`libphonenumber-js`); matching by email first, then phone as the backup (Kd, RULINGS 2026-09-17). Only staff who can manage members see it; a person who never joined and drops off the list is deleted; the list goes when the gym closes. Tests: real exports (Excel, Google Sheets, LibreOffice, semicolon files, accented names), every matching case in one table, the stranger test on every route, broken and oversized files refused. No screen; ticked on merge; the reviewer runs it with several gyms and staff.
   - 3b. [ ] **The invites.** Each new person on the list is emailed once, "you're a member of {gym} — get the app", with an unsubscribe link, from an address of its own so a stale list can never get sign-in codes blocked (a one-time domain setting for Kd); an invited person who signs in with that email joins. One email per invite (Resend: free 100 a day; Pro $20 a month for 50,000, read 2026-09-17). Its plan asks Kd whether that person joins with one tap or none. **Planned 2026-09-19 (spec Part 3 §9.12; Opus xhigh). AMENDED the same day on Kd's words (RULINGS 2026-09-19): nothing is sent when a list is confirmed — staff press Invite, for everyone or for the status words they tick, and the button says how many it will reach before it sends; adding one person by hand offers "Add and invite"; a person joins by signing in with their address only where the gym INVITED that address (being on the list is not enough — a gym that invited its Active members has not said yes to the rest), everyone else uses the code; its plan asks Kd whether staff may send again to ONE person who asks.** One email per person per gym EVER and no reminders; a record keyed by a hash of the address, so a broken export then a good one emails nobody twice; unsubscribes per gym, hard bounces for every gym; a queue with a first batch of 50, daily caps, a stop at 2 % bounces, one cap for the whole platform; the message fixed, only the gym's name in it; the join link is not a key — joining needs a sign-in with that address; the gym's postal address in the footer (a new Settings field; US and Canadian law want it). **The email provider's limits (Resend's rules, read 2026-09-19: an account can be closed "without warning" past 4 % bounces or 0.08 % complaints, and sign-in codes use the same account) are met the standard way and no other (RULINGS 2026-09-19 — a second account and asking Resend's permission were refused by Kd as invented and uncertain; struck):** invites from a sub-domain of their own; every address checked before it is emailed; small batches; a gym's sending stopped when its bounces pass 2 %; a dead address never emailed twice; one-click unsubscribe; and the join link can also be copied and shared by the gym itself (spec §4.3's Invite sheet). The email sender already sits behind one swap point, so moving to another provider is a settings change. Recommended for "one tap or none": ONE tap, on a screen showing what the gym can see.
   - 3c. [ ] **The code admits at once**, marked "not on your list yet"; the member is asked their phone number as the gym has it; the waiting room, its expiry, reminders and nudge off, not deleted; the join door, setup's code screen and My Gyms say "You're in". Its plan asks Kd what a FULL gym's door does. **Planned 2026-09-19 (spec Part 3 §9.13; Opus xhigh):** "not on your list yet" is shown to the GYM only and no reply to a member ever carries it, so the phone question cannot be used to find out whose number a gym holds; the same reply whether or not the number matches; 5 changes a day a person, with its own limit for a shared gym wi-fi; an invited person joining (3b) meets the same full-gym wall.
   - The line as Kd ruled it (RULINGS 2026-09-17): roster upload (CSV + XLSX, column-mapping preview) that RECONCILES — every upload is the whole list today; new people are invited by email "you're a member of {gym} — get the app" with the join link, and a person who signs in with that email is confirmed on their own (RULINGS 2026-08-18); people missing from the list are flagged, never removed on their own; people who joined by code and are not on the list are shown; matching by email or phone (the roster stores a phone; a member joining by code is asked for their number as the gym has it; no SMS). The join code admits AT ONCE, marked "not on your list yet" (RULINGS 2026-09-17 amends 2026-08-19): the pending application, its 14-day expiry, reminders and nudge go off, not deleted. Also the typed single address. First in this stage.
4. [ ] Staff invites by email; a gym can be handed to another owner; a gym can change its own name, city, timezone.
5. [ ] **The member list, screen half**: the upload's result lands on the Members screen ("12 new, invited · 3 no longer on your list — remove all? · 2 joined by code, not on your list"), with last active, streak, search, CSV export; notify members when confirmed or removed. Planned with 3, built right after it. **Planned 2026-09-19 (spec Part 3 §9.14; Opus xhigh — it holds remove-all and the export):** the list shows the gym's own status words as filter chips with their counts ("Active 312 · Expired 88"), and ONE Invite button that always says who it will reach ("Invite 214 people") and afterwards who was skipped and why; "Add" and "Add and invite" for a walk-in; "Put on the list" on an app member who is not on it; the same words and join link to copy, for a gym that would rather send the invite itself; the result line reads "12 new", not "12 new, invited" (a confirm sends nothing, RULINGS 2026-09-19); the preview is phone-first — counts in words, each opening its names, every column with samples and a dropdown to correct the guess, the file's warnings in plain words; a large change shows its numbers and asks for a typed confirmation; remove-all shows the names before it asks; the export guards names against spreadsheet formulas without breaking a `+44…` phone. **One line to Kd in its plan:** the words of the tick a gym makes before uploading ("I'm allowed to share this list and have my members emailed") — his and the lawyer's.
6. [ ] Gym profile page for members; gym announcements feed; gym greeting and branding on the member home.
7. [ ] Member-to-coach / gym messaging.
8. [ ] Classes, schedules and booking (the largest gym item).
9. [ ] Gyms set their own prices, offers and promos; gyms sell products to members.
10. [ ] Coach-authored workout and diet plans for a named member; coach videos (20 per gym, 1 minute). A gym's diet plan adds **Gym's plan** to the rings' switch (7a-iv-e; RULINGS 2026-09-17).
11. [ ] Photo sharing to the gym (one-week life, reactions only, report-and-remove), the photo/stats editor.
12. [ ] Reports section; billing section buttons; the seat cap re-counted; the two sweeps run in production configuration.
13. ~~The invite card~~ — **moved to Stage 5 item 7** the same day it was planned (Kd, 2026-09-13: *"lets build these things when we develop the mobile app not in web app"*). What the US market does instead, for the main path, is items 3 and 5 here: the owner types or imports members' emails, the app emails "you're a member of {gym} — get the app", and the person who signs in with that email is confirmed on their own (RULINGS 2026-08-18: verified-email match auto-confirms). Kd agreed 2026-09-13 (*"i agree with you"*): those two come FIRST in Stage 2, before the leaderboard — the order note at the top of this stage.
14. [ ] Kd's admin panel — last.
15. [ ] **Gym management features** (Kd, RULINGS 2026-09-19: *"it will be a management app but the management feature we will built later"*). What a gym runs its members with: plans, fees, start, renewal and expiry dates, payments and who has paid, reminders. The member list (items 3 and 5) is its first piece and already serves a gym with no other software; this line adds the rest. Not planned yet: a Fable planning chat first, which also settles with Kd where it sits in the order and how it meets Stage 3 item 3 (the gym marks a member paid) and Stage 2 items 8 and 9 (classes; the gym's own prices). The upload then learns those columns (spec Part 3 §9.11).

## Stage 3 — Money

1. [ ] Paddle checkout for individuals and gyms, ON THE WEB: the phone app's console shows the plan only, no Subscribe button and no link out (RULINGS 2026-09-15); the subscription state machine; webhooks (raw body, dedupe, worker); invoices.
2. **Trials enforced** (split 2026-09-08 after Kd's trial-abuse ruling; no approval queue, ever).
   - 2a. [ ] Trial lengths 7 days individual and 15 days organisation (the button still says 30), one per owner account (built), no card; the price book seeded as ruled — the $79 · $129 · $199 · $279 · $379 ladder with its 200 / 500 / 1,000 / 1,500 / 2,100 boundaries (RULINGS 2026-09-15) replaces the 2026-08-25 USD book still in `seed.ts` and its migration test; the INR book is unchanged; the 7-scan member allowance is already seeded (the scanner card, Stage 4 item 7).
   - 2b. [ ] Same person, new spelling: Gmail dots and `+tags` collapse for trial counting; throwaway email domains refused at the Manage door.
   - 2c. [ ] One device, one trial, whatever the email: a browser mark on the web, the stores on the phone.
   - 2d. [ ] The trial belongs to the gym: a new organisation whose first members were members of an already-trialled organisation gets no second trial and sees the subscribe prompt; the owner's own device counts as member one.
   - 2e. [ ] Allow now, look later: the "have a look" list for weaker clues (an email to Kd until the admin panel; pause and remove already exist) and the second-trial code for a real owner of two gyms.
3. [ ] Gym-collects-from-members: the gym pastes its own payment link (Stripe, Square, anything) into the console, the member's app shows "Pay {gym}", and the gym marks the member paid (RULINGS 2026-09-15; Stripe Connect is closed to a platform in India) — after launch unless a gym asks.

## Stage 4 — Launch readiness

1. [ ] Deploy: a server, a domain, secrets in the platform, automatic deploy from main, health checks, the worker running (DPDP day-14 purge must run before the first real sign-up). The web build sets `VITE_APP_VERSION`, which every consent row stores as the build that showed the words (4b-i); until it does, a build says `web-dev`. The meal scanner's `GEMINI_API_KEY` on Google billing with a monthly budget before the first real user: the free plan lets Google read the photos and cannot serve the EU or the UK (its terms, read 2026-09-15). The USDA food table is loaded once per environment by `apps/api/tools/import-usda.ts` after the migrations run (7a-iii-a), production included.
2. [ ] Backup and restore drill performed; the secrets escrow list.
3. [ ] Consent screens for health data and the camera; terms and privacy notice reviewed by a lawyer; a sole proprietorship — the only form Kd can take (RULINGS 2026-09-15) — and product liability insurance (Kd). Also here: the exercise GIFs and the photos under `apps/web/public` (about 190 files, counted 2026-09-15) came with the old frontend and carry no licence record; check or replace them before launch, since the app shows them to everyone and the repository is public. **Also here, from the member list's plan (2026-09-19; spec Part 3 §9.12):** a gym's uploaded list is other people's data held FOR the gym, so the terms need a data-processing agreement with each gym (GDPR Art 28; standard contractual clauses for an EU gym, since the company is in India), the gym's promise that it may share the list and have its members emailed, and the invite email's footer checked against US, Canadian, UK/EU and Australian rules (the gym's postal address, both names, unsubscribe). Research, not advice — the lawyer decides.
4. [ ] Image storage on R2 (meal photos with 7-day deletion, avatars, gym logos).
5. [ ] The pricing questions still open with Kd: the INR book after the USD raise; the yearly book; "custom above 2,100" has no route in. (The USD bands and the individual's $10 were ruled on 2026-09-15.)
6. [ ] Camera accuracy list (from the archive): rest billed as squatting, a too-shallow squat says nothing, constant voice coaching, 12 vs 15 frames a second, person-check tuned on a different model, degradation ladder, choosing which person is the user, follow-the-demo mode.
7. [x] **The meal scanner on Gemini 3.5 Flash-Lite** (RULINGS 2026-08-24, amended 2026-09-15; found unbuilt 2026-09-14: `MEAL_VISION_MODEL` defaulted to Groq's `qwen/qwen3.6-27b`). *Built 2026-09-15 on branch `gemini-meal-scanner`.* Google refuses 2.5 Flash-Lite to new accounts (HTTP 404), so 3.5 Flash-Lite at low picture detail, the reply trimmed to what the app reads (Kd: *"lets do the trim"*), Groq's model kept as the spare behind `MEAL_VISION_MODEL`. Measured on Kd's eight plates: $0.00068 a scan (Groq $0.00182), the same foods found at every setting. Each model carries its own list price (`MEAL_VISION_MODELS`) and the ledger names it (`gemini:gemini-3.5-flash-lite`); a field the model leaves out reads as unknown and the three retired fields are dropped, not failed; a photo with no meal on it asks for a retake instead of failing; `cuisineGuess` leaves the scan response (nothing read it; RULINGS 2026-09-12); the browser shrinks the photo to 768 px JPEG before upload (`apps/web/src/utils/shrinkPhoto.js`, which also leaves the camera's location tags on the phone); a gym member's allowance is 7 scans a day (the seed and its three tests). Kd's click-through found the matching flaw, which is 7a-iii and not this card's defect; no more click-through of this card is needed. Merged with master after #71 (2026-09-15). A failed scan says why, is logged with its reason, and costs no scan (Part 8 §5.3): an outage (Google's 429 limit, a 5xx, the network, the 30 s timeout) is "Meal scanning is busy right now"; a request Google refuses (any other HTTP error), or a fault anywhere in the scan, is "Meal scanning is unavailable right now", and a fault goes to Sentry with its request id and nothing of the request (no body, cookie, header, query string or breadcrumb: `src/sentry.ts`, the API's and the worker's); what the scan took is given back, the scan it counted or, for a scan that rode a free retry, a new free retry; after three such failures in five minutes that person's scans are refused, taking nothing and calling no provider, until the five minutes are up; a reply that names no food, or cannot be read, asks for a retake; a switched-off scanner counts no scan, and neither does a try refused over the day's limit, so a scan given back can always be used again; only the model's own answer is ledgered; the scanner's reply schemas live in `@app/shared`, where a word that says there is none ("none", "N/A", "null", "unknown") names no meal, container, size or food left out of the total. Merged 2026-09-16 on Kd's word (*"merge"*; PR #72, CI green, branch deleted).
8. [ ] **Packaged products without the live Open Food Facts search.** Its API documentation (read 2026-09-14) allows 10 searches a minute per IP address — our server's one address, for every user together — says not to search as the person types, and asks an app with a lot of traffic to use its free daily export instead. Import the export (1,275,171,186 bytes compressed, measured 2026-09-14; keep products sold in the markets, with nutrition per 100 g) into a table of its own, never mixed into the app's food list, and search it there — loaded once, then refreshed from their newest file once a week by the worker (their file is remade daily; weekly is enough for packaged products); the phone's barcode scan (Stage 5 item 5) may ask their product API from each phone instead; send the User-Agent it asks for (app name, version, a contact). The Open Database License, read the same day, allows commercial use: the notice already under the search box (section 4.3) stays, and a copy built from the export must come with a free public file giving the method of making it — the import's filter (section 4.6); the app's own list beside it stays the app's (4.5).
9. [ ] **The app becomes 18+** (Kd, RULINGS 2026-09-15: Google's Gemini terms forbid an app likely to be used by under-18s, and the scanner runs on Gemini). The age wheel on "About you" and Settings' age box refuse an age under 18 with a plain line; the sign-up note and the store listings (Stage 5 item 6) say 18+; the under-18 plan rules (no calorie cut, the teen healthy weight) stay in the code as a floor, since a typed age cannot be checked. No migration: production launches empty (RULINGS 2026-07-13). A small card, its own chat.

## Stage 5 — The phone app

1. [ ] Expo dev client; the two-day pose spike (≥20 fps on a mid-range Android is the gate).
2. [ ] Screens on shared packages: sign-in (code, Google, Apple), "Before you start" (4d: the phone reads the profile's `signUpDisclaimerAgreed` too, or it skips the note; never before the console), onboarding v2, Today, goals, plans, library, camera workout, progress, My Gyms, console.
3. [ ] Offline SQLite mirror and the sync engine.
4. [ ] Running end to end: MapLibre + OpenStreetMap tiles, background tracking, the Play permission flow, routes, published routes only.
5. [ ] Meal scan and barcode on the phone.
6. [ ] Store billing (RevenueCat) for an individual buying in the phone app; the phone console shows the plan only, subscriptions are bought on the web (RULINGS 2026-09-15); push, EAS release trains, store listings (rated 18+, RULINGS 2026-09-15) and privacy labels, Apple small-business programme.
7. [ ] **The invite card** (Kd, 2026-09-13; RULINGS the same day, amended twice: built with the phone app, not on the web now; the app's QR stays). An organisation downloads it from the console (web and phone — the console is on both) and puts it up in the gym; never shared on social media; Copy stays for sending the code personally. **On it (Kd's words):** the organisation's logo big, or its name in big letters when it has none (Stage 4 item 4 brings the upload; `gyms.logo_key` exists and nothing writes it) · a large QR to JOIN, holding `/org/join?code=…` — the address 4b-ii-b made work for everyone — with "Scan to join {name}" and, under it, "or type the code {CODE} in the app" (the chat's choice: the join box's own hint says "read it off their poster", and not every phone scans) · a QR EACH for the Android and iPhone downloads (RULINGS 2026-09-13), each AS BIG as the join QR (Kd: a small one is no use, and a member with no app can use nothing), with the app's mark and its name "AI Home Gym" — built here, at Stage 5, the store listings (item 6) exist, so each opens a real store page. White with the app's orange so it prints cheaply; the organisation's logo or name the biggest thing on it, the app's smaller; premium, no emoji; rendered headless and looked at before Kd sees it. Made in the browser from what the console already reads; A4 at print quality; the button offered only for a live code, and Rotate says a printed card goes dead with the old code. Two free packages, both MIT, needing Kd's yes then: `qrcode-generator` (draws the QR; no dependencies of its own) and `jsqr` (tests only: reads each QR back the way a phone does, so a test proves the join QR opens the join address and each store QR its store). Tests: the card's words and what it carries from the organisation's row; all three QRs decode to their exact addresses; the button withheld for a paused, expired or used-up code; the download makes a PNG.
8. [ ] **Phone check-in** (RULINGS 2026-09-17): the phone notices it is at the gym — a fence around the gym's address, which the gym types once in Settings — and asks "At {gym}? Tap to check in": one prompted tap that cannot be sent from home, recorded as a visit with its own source beside the tap and the QR, so it feeds active days (Stage 2 item 1a) unchanged. Needs the location permission and says why; nothing stored beyond the visit, no route. The QR poster stays as the backup for a gym without its own entry system.

## After launch

- The Pact (random matching, squads, identity reveal, joint photo) · territory capture (individual perk, 200 m end-trim on routes) · published routes and the running social layer · wearables both ways · the global leaderboard · City League · TV mode · share cards · nearby gyms · a map of gyms · exercises 4–58 through the production line (12 solid before pitching) · Hindi and Assamese exercise text · the eight residual coach items (only if the coach ever returns).
- Food, after launch (Kd, 2026-09-16; RULINGS the same day): **a diet-quality score** for a day, on a published index — HEI-2020 (USDA's Center for Nutrition Policy and Promotion and the National Cancer Institute), which needs USDA's Food Patterns Equivalents Database (public domain, keyed to the same FNDDS foods 7a-iii-a imports); its plan names the scoring standards and checks them against the published text; never Nutri-Score (per product, a trademark) · **"Cooked how?"**: a scanned row offers the same food's other USDA cooking entries (baked, fried, boiled, raw) and a one-tap "cooked in oil" adds a tablespoon of oil as an ingredient. No model call for either.
- Gyms' own entry systems (RULINGS 2026-09-17, with that day's research): **door machines that send each entry to our server** — eSSL/ZKTeco first (their "Cloud Server" setting holds ONE server address, so a gym already on another cloud gym app must choose, and the setup screen says so), then Matrix COSEC; built when an Indian gym asks, in two chats: the server that receives entries from registered machines only (a number and a time, never a fingerprint, face or photo; anything else a machine sends is dropped on arrival; entries for people not in the app are dropped, never stored), then the console screen that registers a machine and links members to their machine number (from the upload, when the gym's export carries it). Reviewed by RUNNING a real machine, which has to be bought or borrowed first. Abroad, when a paying gym has one: Kisi (self-serve key, live entry events, sells to gyms), Verkada, Avigilon Alta; Brivo's live key needs their team; Salto KS, HID, Paxton and Gantner are gated or undocumented. **Live connections to gym software** (Mindbody's visits API, Glofox, ClubReady, Zen Planner, eGym), one vendor at a time, each behind that vendor's partner approval. The app never controls a door before it tracks payment (Stage 3 item 3), and a gym can let a blocked member back in from the console at once.
- The member list, after launch (from its plan, 2026-09-19; spec Part 3 §9.11 lists each limit): reading a legacy `.xls` file directly (today it is refused with the words "save it as .xlsx or CSV"; many older Indian desktop gym programs still write it) · decoding a CSV saved as "CSV (MS-DOS)" (today it is warned about) · skipping a sheet's hidden rows (today they are read, with a warning) · matching across Gmail dots and `+tags`, once Stage 3 item 2b has built the one shared rule · each only when a real gym's file needs it.
- Undecided, do not build: day passes · paid friend/family invites · training together live · nearby-runner connection.

## Ruled out — never build, never re-propose

The AI chat coach (off, not deleted) · per-seat pricing · phone OTP at sign-up · gym-team territory · the eight struck gym ideas (see RULINGS) · session names · join-code labels · user-added exercises · Google Maps · desktop webcam capture for meal photos · clinics as an organisation type · Kd approval before a gym can start · a pooled route heatmap.
